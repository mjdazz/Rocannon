SYNOPSIS                   *na_elementsw_network_interfaces*

     • Configure Element SW Node Network Interfaces for Bond 1G and 10G IP address.

REQUIREMENTS                   *na_elementsw_network_interfaces-requirements*

   The below requirements are needed on the host that executes this module.

     • The modules were developed with SolidFire 10.1
     • solidfire-sdk-python (1.1.0.92) or greater. Install using ‘pip install solidfire-sdk-python’

PARAMETERS                   *na_elementsw_network_interfaces-parameters*

        Parameter       Choices/Defaults                                                                            Comments
                         Choices:
   bond_mode_10g         • ActivePassive ← Bond mode for 10GbE configuration.
   -                     • ALB
                         • LACP
                         Choices:
   bond_mode_1g          • ActivePassive ← Bond mode for 1GbE configuration.
   -                     • ALB
                         • LACP
   dns_nameservers                         List of addresses for domain name servers.
   -
   dns_search_domains                      List of DNS search domains.
   -
   gateway_address_10g                     Router network address to send packets out of the local network.
   - / required
   gateway_address_1g                      Router network address to send packets out of the local network.
   - / required
   hostname                                The hostname or IP address of the SolidFire cluster.
   - / required
   ip_address_10g                          IP address for the 10G network.
   - / required
   ip_address_1g                           IP address for the 1G network.
   - / required
   lacp_10g              Choices:          Link Aggregation Control Protocol useful only if LACP is selected as the Bond Mode.
   -                     • Fast            Slow - Packets are transmitted at 30 second intervals.
                         • Slow ←          Fast - Packets are transmitted in 1 second intervals.
   lacp_1g               Choices:          Link Aggregation Control Protocol useful only if LACP is selected as the Bond Mode.
   -                     • Fast            Slow - Packets are transmitted at 30 second intervals.
                         • Slow ←          Fast - Packets are transmitted in 1 second intervals.
                         Choices:          Type of Method used to configure the interface.
                         • loopback        method depends on other settings such as the use of a static IP address, which will change the method to static.
   method                • manual          loopback - Used to define the IPv4 loopback interface.
   - / required          • dhcp            manual - Used to define interfaces for which no configuration is done by default.
                         • static          dhcp - May be used to obtain an IP address via DHCP.
                                           static - Used to define Ethernet interfaces with statically allocated IPv4 addresses.
   mtu_10g             Default:            Maximum Transmission Unit for 10GbE, Largest packet size that a network protocol can transmit.
   -                   "1500"              Must be greater than or equal to 1500 bytes.
   mtu_1g              Default:            Maximum Transmission Unit for 1GbE, Largest packet size that a network protocol can transmit.
   -                   "1500"              Must be greater than or equal to 1500 bytes.
   password                                Password for the specified user.
   - / required                            aliases: pass
   subnet_10g                              10GbE Subnet Mask.
   - / required
   subnet_1g                               1GbE Subnet Mask.
   - / required
   username                                Please ensure that the user has the adequate permissions. For more information, please read the official documentation
   - / required                            https://mysupport.netapp.com/documentation/docweb/index.html?productID=62636&language=en-US.
                                           aliases: user
   virtual_network_tag                     This is the primary network tag. All nodes in a cluster have the same VLAN tag.
   -

NOTES                   *na_elementsw_network_interfaces-notes*

   Note

     • The modules prefixed with na\_elementsw are built to support the SolidFire storage platform.

EXAMPLES                   *na_elementsw_network_interfaces-examples*

 - name: Set Node network interfaces configuration for Bond 1G and 10G properties
   tags:
   - elementsw_network_interfaces
   na_elementsw_network_interfaces:
     hostname: "{{ elementsw_hostname }}"
     username: "{{ elementsw_username }}"
     password: "{{ elementsw_password }}"
     method: static
     ip_address_1g: 10.226.109.68
     ip_address_10g: 10.226.201.72
     subnet_1g: 255.255.255.0
     subnet_10g: 255.255.255.0
     gateway_address_1g: 10.193.139.1
     gateway_address_10g: 10.193.140.1
     mtu_1g: 1500
     mtu_10g: 9000
     bond_mode_1g: ActivePassive
     bond_mode_10g: LACP
     lacp_10g: Fast

RETURN VALUES                   *na_elementsw_network_interfaces-return values*

   Common return values are documented here, the following are the fields unique to this module:

    Key   Returned                                                                                    Description
   msg    success  Success message
   string

STATUS                   *na_elementsw_network_interfaces-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by an Ansible Partner. [certified]

     • NetApp Ansible Team (@carchi8py) <ng-ansibleteam@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_elementsw_network_interfaces-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_elementsw_network_interfaces_module.html
