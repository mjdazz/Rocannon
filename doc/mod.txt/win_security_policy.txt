SYNOPSIS                   *win_security_policy*

     • Allows you to set the local security policies that are configured by SecEdit.exe.

PARAMETERS                   *win_security_policy-parameters*

       Parameter     Choices/Defaults                                                                            Comments
   key                                The ini key of the section or policy name to modify.
   string / required                  The module will return an error if this key is invalid.
                                      The ini section the key exists in.
   section                            If the section does not exist then the module will return an error.
   string / required                  Example sections to use are 'Account Policies', 'Local Policies', 'Event Log', 'Restricted Groups', 'System Services', 'Registry' and 'File System'
                                      If wanting to edit the Privilege Rights section, use the win_user_right module instead.
   value                              The value for the ini key or policy name.
   string / required                  If the key takes in a boolean value then 0 = False and 1 = True.

NOTES                   *win_security_policy-notes*

   Note

     • This module uses the SecEdit.exe tool to configure the values, more details of the areas and keys that can be configured can be found here
       https://msdn.microsoft.com/en-us/library/bb742512.aspx.
     • If you are in a domain environment these policies may be set by a GPO policy, this module can temporarily change these values but the GPO will override it if the value differs.
     • You can also run SecEdit.exe /export /cfg C:\temp\output.ini to view the current policies set on your system.
     • When assigning user rights, use the win_user_right module instead.

SEE ALSO                   *win_security_policy-see also*

   See also

   win_user_right – Manage Windows User Rights
   The official documentation on the win_user_right module.

EXAMPLES                   *win_security_policy-examples*

 - name: Change the guest account name
   win_security_policy:
     section: System Access
     key: NewGuestName
     value: Guest Account

 - name: Set the maximum password age
   win_security_policy:
     section: System Access
     key: MaximumPasswordAge
     value: 15

 - name: Do not store passwords using reversible encryption
   win_security_policy:
     section: System Access
     key: ClearTextPassword
     value: 0

 - name: Enable system events
   win_security_policy:
     section: Event Audit
     key: AuditSystemEvents
     value: 1

RETURN VALUES                   *win_security_policy-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key                      Returned                                                                                   Description
   import_log                                             The log of the SecEdit.exe /configure job that configured the local policies. This is used for debugging purposes on failures.
   string     secedit.exe /import run and change occurred Sample:
                                                          Completed 6 percent (0/15) \tProcess Privilege Rights area.
   key                                                    The key in the section passed to the module to modify.
   string     success                                     Sample:
                                                          NewGuestName
   rc                                                     The return code after a failure when running SecEdit.exe.
   integer    failure with secedit calls                  Sample:
                                                          -1
   section                                                The section passed to the module to modify.
   string     success                                     Sample:
                                                          System Access
   stderr                                                 The output of the STDERR buffer after a failure when running SecEdit.exe.
   string     failure with secedit calls                  Sample:
                                                          failed to import security policy
   stdout                                                 The output of the STDOUT buffer after a failure when running SecEdit.exe.
   string     failure with secedit calls                  Sample:
                                                          check log for error details
   value                                                  The value passed to the module to modify to.
   string     success                                     Sample:
                                                          Guest Account

STATUS                   *win_security_policy-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Jordan Borean (@jborean93)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *win_security_policy-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/win_security_policy_module.html
