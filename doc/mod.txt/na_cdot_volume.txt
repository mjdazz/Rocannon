DEPRECATED                   *na_cdot_volume*

                            Removed in Ansible:
                  version: 2.11
        Why:      Updated modules released with increased functionality
    Alternative:  Use na_ontap_volume instead.

SYNOPSIS                   *na_cdot_volume-synopsis*

     • Create or destroy volumes on NetApp cDOT

REQUIREMENTS                   *na_cdot_volume-requirements*

   The below requirements are needed on the host that executes this module.

     • A physical or virtual clustered Data ONTAP system. The modules were developed with Clustered Data ONTAP 8.3
     • Ansible 2.2
     • netapp-lib (2015.9.25). Install using ‘pip install netapp-lib’

PARAMETERS                   *na_cdot_volume-parameters*

      Parameter    Choices/Defaults                                                                             Comments
   aggregate_name                   The name of the aggregate the flexvol should exist on. Required when state=present.
   -
   export_policy   Default:
   -               "default"        Export policy to set for the specified junction path.
   added in 2.6
   hostname                         The hostname or IP address of the ONTAP instance.
   - / required
   infinite          Choices:
   boolean           • no ←         Set True if the volume is an Infinite Volume.
                     • yes
   junction_path
   -                                Junction path where to mount the volume
   added in 2.6
   name                             The name of the volume to manage.
   - / required
   online            Choices:
   boolean           • no           Whether the specified volume is online, or not.
                     • yes ←
   password                         Password for the specified user.
   - / required                     aliases: pass
   size                             The size of the volume in (size_unit). Required when state=present.
   -
                     Choices:
                     • bytes
                     • b
                     • kb
   size_unit         • mb
   -                 • gb ←         The unit used to interpret the size parameter.
                     • tb
                     • pb
                     • eb
                     • zb
                     • yb
   snapshot_policy Default:
   -               "default"        Snapshot policy to set for the specified volume.
   added in 2.6
   state             Choices:
   - / required      • present      Whether the specified volume should exist or not.
                     • absent
   username                         This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required. For more information, please read the
   - / required                     documentation https://mysupport.netapp.com/NOW/download/software/nmsdk/9.4/.
                                    aliases: user
   vserver                          Name of the vserver to use.
   - / required

NOTES                   *na_cdot_volume-notes*

   Note

     • The modules prefixed with na\_cdot are built to support the ONTAP storage platform.

EXAMPLES                   *na_cdot_volume-examples*

 - name: Create FlexVol
   na_cdot_volume:
     state: present
     name: ansibleVolume
     infinite: False
     aggregate_name: aggr1
     size: 20
     size_unit: mb
     vserver: ansibleVServer
     hostname: "{{ netapp_hostname }}"
     username: "{{ netapp_username }}"
     password: "{{ netapp_password }}"
     junction_path: /ansibleVolume
     export_policy: all_nfs_networks
     snapshot_policy: daily

 - name: Make FlexVol offline
   na_cdot_volume:
     state: present
     name: ansibleVolume
     infinite: False
     online: False
     vserver: ansibleVServer
     hostname: "{{ netapp_hostname }}"
     username: "{{ netapp_username }}"
     password: "{{ netapp_password }}"

STATUS                   *na_cdot_volume-status*

     • This module will be removed in version 2.11. [deprecated]
     • For more information see DEPRECATED.

     • Sumit Kumar (@timuster) <sumit4@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_cdot_volume-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_cdot_volume_module.html
