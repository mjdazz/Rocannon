DEPRECATED                   *na_cdot_user*

                            Removed in Ansible:
                  version: 2.11
        Why:      Updated modules released with increased functionality
    Alternative:  Use na_ontap_user instead.

SYNOPSIS                   *na_cdot_user-synopsis*

     • Create or destroy users.

REQUIREMENTS                   *na_cdot_user-requirements*

   The below requirements are needed on the host that executes this module.

     • A physical or virtual clustered Data ONTAP system. The modules were developed with Clustered Data ONTAP 8.3
     • Ansible 2.2
     • netapp-lib (2015.9.25). Install using ‘pip install netapp-lib’

PARAMETERS                   *na_cdot_user-parameters*

         Parameter       Choices/Defaults                                                                          Comments
                           Choices:
                           • console
                           • http
   application             • ontapi
   - / required            • rsh          Applications to grant access to.
                           • snmp
                           • sp
                           • ssh
                           • telnet
                                          Authentication method for the application.
                                          Not all authentication methods are valid for an application.
                           Choices:       Valid authentication methods for each application are as denoted in authentication_choices_description.
                           • community    password for console application
                           • password     password, domain, nsswitch, cert for http application.
   authentication_method   • publickey    password, domain, nsswitch, cert for ontapi application.
   - / required            • domain       community for snmp application (when creating SNMPv1 and SNMPv2 users).
                           • nsswitch     usm and community for snmp application (when creating SNMPv3 users).
                           • usm          password for sp application.
                                          password for rsh application.
                                          password for telnet application.
                                          password, publickey, domain, nsswitch for ssh application.
   hostname                               The hostname or IP address of the ONTAP instance.
   - / required
   name                                   The name of the user to manage.
   - / required
   password                               Password for the specified user.
   - / required                           aliases: pass
   role_name                              The name of the role. Required when state=present
   -
   set_password                           Password for the user account.
   -                                      It is ignored for creating snmp users, but is required for creating non-snmp users.
                                          For an existing user, this value will be used as the new password.
   state                   Choices:
   - / required            • present      Whether the specified user should exist or not.
                           • absent
   username                               This can be a Cluster-scoped or SVM-scoped account, depending on whether a Cluster-level or SVM-level API is required. For more information, please read
   - / required                           the documentation https://mysupport.netapp.com/NOW/download/software/nmsdk/9.4/.
                                          aliases: user
   vserver                                The name of the vserver to use.
   - / required

NOTES                   *na_cdot_user-notes*

   Note

     • The modules prefixed with na\_cdot are built to support the ONTAP storage platform.

EXAMPLES                   *na_cdot_user-examples*

 - name: Create User
   na_cdot_user:
     state: present
     name: SampleUser
     application: ssh
     authentication_method: password
     set_password: apn1242183u1298u41
     role_name: vsadmin
     vserver: ansibleVServer
     hostname: "{{ netapp_hostname }}"
     username: "{{ netapp_username }}"
     password: "{{ netapp_password }}"

STATUS                   *na_cdot_user-status*

     • This module will be removed in version 2.11. [deprecated]
     • For more information see DEPRECATED.

     • Sumit Kumar (@timuster) <sumit4@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_cdot_user-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_cdot_user_module.html
