SYNOPSIS                   *gitlab_project*

     • When the project does not exist in GitLab, it will be created.
     • When the project does exists and state=absent, the project will be deleted.
     • When changes are made to the project, the project will be updated.

REQUIREMENTS                   *gitlab_project-requirements*

   The below requirements are needed on the host that executes this module.

     • python >= 2.7
     • python-gitlab python module <= 1.12.1

PARAMETERS                   *gitlab_project-parameters*

         Parameter        Choices/Defaults                                                                          Comments
   api_password                            The password to use for authentication against the API
   string
   api_token                               GitLab token for logging in.
   string                                  aliases: login_token
   api_url                                 The resolvable endpoint for the API
   string
   api_username                            The username to use for authentication against the API
   string
   description                             An description for the project.
   string
   group                                   Id or The full path of the group of which this projects belongs to.
   string
   import_url                              Git repository which will be imported into gitlab.
   string                                  GitLab server needs read access to this git repository.
   issues_enabled           Choices:       Whether you want to create issues or not.
   boolean                  • no           Possible values are true and false.
                            • yes ←
   login_password                          GitLab password for login_user
   string
   login_user                              GitLab user name.
   string
   merge_requests_enabled   Choices:       If merge requests can be made or not.
   boolean                  • no           Possible values are true and false.
                            • yes ←
   name                                    The name of the project
   string / required
   path                                    The path of the project you want to create, this will be server_url/<group>/path
   string                                  If not supplied, name will be used.
   server_url                              The URL of the GitLab server, with protocol (i.e. http or https).
   string
   snippets_enabled         Choices:       If creating snippets should be available or not.
   boolean                  • no           Possible values are true and false.
                            • yes ←
   state                    Choices:       create or delete project.
   string                   • present ←    Possible values are present and absent.
                            • absent
   validate_certs           Choices:
   boolean                  • no           Whether or not to validate SSL certs when supplying a https endpoint.
                            • yes ←
                            Choices:       Private. Project access must be granted explicitly for each user.
   visibility               • private ←    Internal. The project can be cloned by any logged in user.
   string                   • internal     Public. The project can be cloned without any authentication.
                            • public       aliases: visibility_level
   wiki_enabled             Choices:       If an wiki for this project should be available or not.
   boolean                  • no           Possible values are true and false.
                            • yes ←

EXAMPLES                   *gitlab_project-examples*

 - name: Delete GitLab Project
   gitlab_project:
     api_url: https://gitlab.example.com/
     api_token: "{{ access_token }}"
     validate_certs: False
     name: my_first_project
     state: absent
   delegate_to: localhost

 - name: Create GitLab Project in group Ansible
   gitlab_project:
     api_url: https://gitlab.example.com/
     validate_certs: True
     api_username: dj-wasabi
     api_password: "MySecretPassword"
     name: my_first_project
     group: ansible
     issues_enabled: False
     wiki_enabled: True
     snippets_enabled: True
     import_url: http://git.example.com/example/lab.git
     state: present
   delegate_to: localhost

RETURN VALUES                   *gitlab_project-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   error               the error message returned by the GitLab API
   string     failed   Sample:
                       400: path is already in use
   msg                 Success or failure message
   string     always   Sample:
                       Success
   project    always   API object
   dictionary
   result     always   json parsed response from the server
   dictionary

STATUS                   *gitlab_project-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Werner Dijkerman (@dj-wasabi)
     • Guillaume Martinez (@Lunik)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *gitlab_project-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/gitlab_project_module.html
