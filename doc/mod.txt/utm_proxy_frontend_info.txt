SYNOPSIS                   *utm_proxy_frontend_info*

     • Create, update or destroy a reverse_proxy frontend entry in SOPHOS UTM.
     • This module needs to have the REST Ability of the UTM to be activated.

PARAMETERS                   *utm_proxy_frontend_info-parameters*

     Parameter    Choices/Defaults                                                                              Comments
   headers                         A dictionary of additional headers to be sent to POST and PUT requests.
   dictionary                      Is needed for some modules
   name                            The name of the object. Will be used to identify the entry
   - / required
   state            Choices:       The desired state of the object.
   string           • absent       present will create or update an object
                    • present ←    absent will delete an object if it was present
   utm_host
   string /                        The REST Endpoint of the Sophos UTM.
   required
   utm_port       Default:         The port of the REST interface.
   integer        4444
   utm_protocol     Choices:
   string           • http         The protocol of the REST Endpoint.
                    • https ←
   utm_token                       The token used to identify at the REST-API. See https://www.sophos.com/en-us/medialibrary/PDFs/documentation/UTMonAWS/Sophos-UTM-RESTful-API.pdf?la=en, Chapter
   string /                        2.4.2.
   required
   validate_certs   Choices:
   boolean          • no           Whether the REST interface's ssl certificate should be verified or not.
                    • yes ←

EXAMPLES                   *utm_proxy_frontend_info-examples*

 - name: Get utm proxy_frontend
   utm_proxy_frontend_info:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestBackendEntry
     host: REF_OBJECT_STRING

RETURN VALUES                   *utm_proxy_frontend_info-return values*

   Common return values are documented here, the following are the fields unique to this module:

              Key            Returned                                                                           Description
   result                    success  The utm object that was created
   complex
     _locked                          Whether or not the object is currently locked
     boolean
     _ref                             The reference name of the object
     string
     _type                            The type of the object
     string
     add_content_type_header          Whether to add the content type header
     boolean
     address                          The reference name of the address
     string
     allowed_networks                 List of reference names of networks associated
     list
     certificate                      Reference name of certificate (ca/host_key_cert)
     string
     comment                          The comment string
     string
     disable_compression              State of compression support
     boolean
     domain                           List of hostnames
     list
     exceptions                       List of associated proxy exceptions
     list
     htmlrewrite                      State of html rewrite
     boolean
     htmlrewrite_cookies              whether the html rewrite cookie will be set
     boolean
     implicitredirect                 whether to use implicit redirection
     boolean
     lbmethod                         The method of loadbalancer to use
     string
     locations                        The reference names of reverse_proxy/locations associated with the object
     list
     name                             The name of the object
     string
     port                             The port of the frontend connection
     integer
     preservehost                     Preserve host header
     boolean
     profile                          The associated reverse_proxy/profile
     string
     status                           Whether the frontend object is active or not
     boolean
     type                             The connection type
     string
     xheaders                         The xheaders state
     boolean

STATUS                   *utm_proxy_frontend_info-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Johannes Brunswicker (@MatrixCrawler)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *utm_proxy_frontend_info-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/utm_proxy_frontend_info_module.html
