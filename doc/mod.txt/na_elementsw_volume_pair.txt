SYNOPSIS                   *na_elementsw_volume_pair*

     • Create, delete volume pair

REQUIREMENTS                   *na_elementsw_volume_pair-requirements*

   The below requirements are needed on the host that executes this module.

     • The modules were developed with SolidFire 10.1
     • solidfire-sdk-python (1.1.0.92) or greater. Install using ‘pip install solidfire-sdk-python’

PARAMETERS                   *na_elementsw_volume_pair-parameters*

     Parameter   Choices/Defaults                                                                               Comments
   dest_account                    Destination account name or ID
   - / required
   dest_mvip                       Destination IP address of the paired cluster.
   - / required
   dest_password                   Destination password for the paired cluster
   -                               Optional if this is same as source cluster password.
   dest_username                   Destination username for the paired cluster
   -                               Optional if this is same as source cluster username.
   dest_volume                     Destination volume name or volume ID
   - / required
   hostname                        The hostname or IP address of the SolidFire cluster.
   - / required
                   Choices:
   mode            • async ←       Mode to start the volume pairing
   -               • sync
                   • snapshotsonly
   password                        Password for the specified user.
   - / required                    aliases: pass
   src_account                     Source account name or ID
   - / required
   src_volume                      Source volume name or volume ID
   - / required
   state           Choices:
   -               • present ←     Whether the specified volume pair should exist or not.
                   • absent
   username                        Please ensure that the user has the adequate permissions. For more information, please read the official documentation
   - / required                    https://mysupport.netapp.com/documentation/docweb/index.html?productID=62636&language=en-US.
                                   aliases: user

NOTES                   *na_elementsw_volume_pair-notes*

   Note

     • The modules prefixed with na\_elementsw are built to support the SolidFire storage platform.

EXAMPLES                   *na_elementsw_volume_pair-examples*

 - name: Create volume pair
   na_elementsw_volume_pair:
     hostname: "{{ src_cluster_hostname }}"
     username: "{{ src_cluster_username }}"
     password: "{{ src_cluster_password }}"
     state: present
     src_volume: test1
     src_account: test2
     dest_volume: test3
     dest_account: test4
     mode: sync
     dest_mvip: "{{ dest_cluster_hostname }}"

 - name: Delete volume pair
   na_elementsw_volume_pair:
     hostname: "{{ src_cluster_hostname }}"
     username: "{{ src_cluster_username }}"
     password: "{{ src_cluster_password }}"
     state: absent
     src_volume: 3
     src_account: 1
     dest_volume: 2
     dest_account: 1
     dest_mvip: "{{ dest_cluster_hostname }}"
     dest_username: "{{ dest_cluster_username }}"
     dest_password: "{{ dest_cluster_password }}"

STATUS                   *na_elementsw_volume_pair-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by an Ansible Partner. [certified]

     • NetApp Ansible Team (@carchi8py) <ng-ansibleteam@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_elementsw_volume_pair-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_elementsw_volume_pair_module.html
