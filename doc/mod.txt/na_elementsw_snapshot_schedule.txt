SYNOPSIS                   *na_elementsw_snapshot_schedule*

     • Create, destroy, or update accounts on ElementSW

REQUIREMENTS                   *na_elementsw_snapshot_schedule-requirements*

   The below requirements are needed on the host that executes this module.

     • The modules were developed with SolidFire 10.1
     • solidfire-sdk-python (1.1.0.92) or greater. Install using ‘pip install solidfire-sdk-python’

PARAMETERS                   *na_elementsw_snapshot_schedule-parameters*

          Parameter             Choices/Defaults                                                                          Comments
                                                       Account ID for the owner of this volume.
   account_id                                          It accepts either account_name or account_id
   -                                                   if account_id is digit, it will consider as account_id
                                                       If account_id is string, it will consider as account_name
   days_of_month_hours     Default:                    Time specified in hours
   -                       0
   days_of_month_minutes   Default:                    Time specified in minutes.
   -                       0
   days_of_month_monthdays                             List of days of the month (1-31)
   -
   days_of_week_hours      Default:                    Time specified in hours
   -                       0
   days_of_week_minutes    Default:                    Time specified in minutes.
   -                       0
   days_of_week_weekdays                               List of days of the week (Sunday to Saturday)
   -
   hostname                                            The hostname or IP address of the SolidFire cluster.
   - / required
                                                       Name for the snapshot schedule.
   name                                                It accepts either schedule_id or schedule_name
   -                                                   if name is digit, it will consider as schedule_id
                                                       If name is string, it will consider as schedule_name
   password                                            Element SW access account password
   - / required                                        aliases: pass
   paused                    Choices:
   boolean                   • no                      Pause / Resume a schedule.
                             • yes
   recurring                 Choices:
   boolean                   • no                      Should the schedule recur?
                             • yes
   retention                                           Retention period for the snapshot.
   -                                                   Format is 'HH:mm:ss'.
                             Choices:
   schedule_type             • DaysOfWeekFrequency     Schedule type for creating schedule.
   -                         • DaysOfMonthFrequency
                             • TimeIntervalFrequency
   snapshot_name                                       Name for the created snapshots.
   -
   starting_date                                       Starting date for the schedule.
   -                                                   Required when state=present.
                                                       Format: 2016-12-01T00:00:00Z
   state                     Choices:
   - / required              • present                 Whether the specified schedule should exist or not.
                             • absent
   time_interval_days      Default:                    Time interval in days.
   -                       1
   time_interval_hours     Default:                    Time interval in hours.
   -                       0
   time_interval_minutes   Default:                    Time interval in minutes.
   -                       0
   username                                            Element SW access account user-name
   - / required                                        aliases: user
   volumes                                             Volume IDs that you want to set the snapshot schedule for.
   -                                                   It accepts both volume_name and volume_id

NOTES                   *na_elementsw_snapshot_schedule-notes*

   Note

     • The modules prefixed with na\_elementsw are built to support the SolidFire storage platform.

EXAMPLES                   *na_elementsw_snapshot_schedule-examples*

 - name: Create Snapshot schedule
   na_elementsw_snapshot_schedule:
     hostname: "{{ elementsw_hostname }}"
     username: "{{ elementsw_username }}"
     password: "{{ elementsw_password }}"
     state: present
     name: Schedule_A
     schedule_type: TimeIntervalFrequency
     time_interval_days: 1
     starting_date: '2016-12-01T00:00:00Z'
     volumes:
     - 7
     - test
     account_id: 1

 - name: Update Snapshot schedule
   na_elementsw_snapshot_schedule:
     hostname: "{{ elementsw_hostname }}"
     username: "{{ elementsw_username }}"
     password: "{{ elementsw_password }}"
     state: present
     name: Schedule_A
     schedule_type: TimeIntervalFrequency
     time_interval_days: 1
     starting_date: '2016-12-01T00:00:00Z'
     volumes:
     - 8
     - test1
     account_id: 1

 - name: Delete Snapshot schedule
   na_elementsw_snapshot_schedule:
     hostname: "{{ elementsw_hostname }}"
     username: "{{ elementsw_username }}"
     password: "{{ elementsw_password }}"
     state: absent
     name: 6

RETURN VALUES                   *na_elementsw_snapshot_schedule-return values*

   Common return values are documented here, the following are the fields unique to this module:

       Key     Returned                                                                                  Description
   schedule_id success  Schedule ID of the newly created schedule
   string

STATUS                   *na_elementsw_snapshot_schedule-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by an Ansible Partner. [certified]

     • NetApp Ansible Team (@carchi8py) <ng-ansibleteam@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_elementsw_snapshot_schedule-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_elementsw_snapshot_schedule_module.html
