SYNOPSIS                   *tower_project*

     • Create, update, or destroy Ansible Tower projects. See https://www.ansible.com/tower for an overview.

REQUIREMENTS                   *tower_project-requirements*

   The below requirements are needed on the host that executes this module.

     • ansible-tower-cli >= 3.0.2

PARAMETERS                   *tower_project-parameters*

          Parameter         Choices/Defaults                                                                         Comments
   custom_virtualenv
   -                                         Local absolute file path containing a custom Python virtualenv to use
   added in 2.8
   description                               Description to use for the project.
   -
   job_timeout              Default:
   -                        0                The amount of time (in seconds) to run before the SCM Update is canceled. A value of 0 means no timeout.
   added in 2.8
   local_path                                The server playbook directory for manual projects.
   -
   name                                      Name to use for the project.
   - / required
   organization                              Primary key of organization for project.
   -
   scm_branch                                The branch to use for the SCM resource.
   -
   scm_clean                  Choices:
   boolean                    • no ←         Remove local modifications before updating.
                              • yes
   scm_credential                            Name of the credential to use with this SCM resource.
   -
   scm_delete_on_update       Choices:
   boolean                    • no ←         Remove the repository completely before updating.
                              • yes
                              Choices:
   scm_type                   • manual ←
   -                          • git          Type of SCM resource.
                              • hg
                              • svn
   scm_update_cache_timeout Default:
   -                        0                Cache Timeout to cache prior project syncs for a certain number of seconds. Only valid if scm_update_on_launch is to True, otherwise ignored.
   added in 2.8
   scm_update_on_launch       Choices:
   boolean                    • no ←         Before an update to the local repository before launching a job with this project.
                              • yes
   scm_url                                   URL of SCM resource.
   -
   state                      Choices:
   -                          • present ←    Desired state of the resource.
                              • absent
   tower_config_file                         Path to the Tower config file.
   path
   tower_host                                URL to your Tower instance.
   string
   tower_password                            Password for your Tower instance.
   string
   tower_username                            Username for your Tower instance.
   string
                              Choices:       Whether to allow insecure connections to Tower.
   validate_certs             • no           If no, SSL certificates will not be validated.
   boolean                    • yes          This should only be used on personally controlled sites using self-signed certificates.
                                             aliases: tower_verify_ssl

NOTES                   *tower_project-notes*

   Note

     • If no config_file is provided we will attempt to use the tower-cli library defaults to find your Tower host information.
     • config_file should contain Tower configuration in the following format host=hostname username=username password=password

EXAMPLES                   *tower_project-examples*

 - name: Add tower project
   tower_project:
     name: "Foo"
     description: "Foo bar project"
     organization: "test"
     state: present
     tower_config_file: "~/tower_cli.cfg"

 - name: Add Tower Project with cache timeout and custom virtualenv
   tower_project:
     name: "Foo"
     description: "Foo bar project"
     organization: "test"
     scm_update_on_launch: True
     scm_update_cache_timeout: 60
     custom_virtualenv: "/var/lib/awx/venv/ansible-2.2"
     state: present
     tower_config_file: "~/tower_cli.cfg"

STATUS                   *tower_project-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Wayne Witzel III (@wwitzel3)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *tower_project-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/tower_project_module.html
