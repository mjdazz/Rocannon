SYNOPSIS                   *gcp_sql_instance_info*

     • Gather info for GCP Instance
     • This module was called gcp_sql_instance_facts before Ansible 2.9. The usage has not changed.

   Aliases: gcp_sql_instance_facts

REQUIREMENTS                   *gcp_sql_instance_info-requirements*

   The below requirements are needed on the host that executes this module.

     • python >= 2.6
     • requests >= 2.18.4
     • google-auth >= 1.3.0

PARAMETERS                   *gcp_sql_instance_info-parameters*

          Parameter           Choices/Defaults                                                                         Comments
                              Choices:
   auth_kind                  • application      The type of credential used.
   string / required          • machineaccount
                              • serviceaccount
   env_type                                      Specifies which Ansible environment you're running this module within.
   string                                        This should not be set unless you know what you're doing.
                                                 This only alters the User Agent string for any API requests.
   project                                       The Google Cloud Platform project to use.
   string
   scopes                                        Array of scopes to be used.
   list
   service_account_contents                      The contents of a Service Account JSON file, either in a dictionary or as a JSON string that represents it.
   jsonarg
   service_account_email                         An optional service account email address if machineaccount is selected and the user does not wish to use the default email.
   string
   service_account_file                          The path of a Service Account JSON file if serviceaccount is selected as type.
   path

NOTES                   *gcp_sql_instance_info-notes*

   Note

     • for authentication, you can set service_account_file using the c(gcp_service_account_file) env variable.
     • for authentication, you can set service_account_contents using the c(GCP_SERVICE_ACCOUNT_CONTENTS) env variable.
     • For authentication, you can set service_account_email using the GCP_SERVICE_ACCOUNT_EMAIL env variable.
     • For authentication, you can set auth_kind using the GCP_AUTH_KIND env variable.
     • For authentication, you can set scopes using the GCP_SCOPES env variable.
     • Environment variables values will only be used if the playbook values are not set.
     • The service_account_email and service_account_file options are mutually exclusive.

EXAMPLES                   *gcp_sql_instance_info-examples*

 - name: get info on an instance
   gcp_sql_instance_info:
     project: test_project
     auth_kind: serviceaccount
     service_account_file: "/tmp/auth.pem"

RETURN VALUES                   *gcp_sql_instance_info-return values*

   Common return values are documented here, the following are the fields unique to this module:

                Key               Returned                                                                        Description
   resources                      always   List of resources
   complex
     backendType                           " FIRST_GEN: First Generation instance. MySQL only.
     string                       success  " SECOND_GEN: Second Generation instance or PostgreSQL instance.
                                           " EXTERNAL: A database server that is not managed by Google.
     connectionName               success  Connection name of the Cloud SQL instance used in connection strings.
     string
     databaseVersion                       The database engine type and version. For First Generation instances, can be MYSQL_5_5, or MYSQL_5_6. For Second Generation instances, can be MYSQL_5_6 or
     string                       success  MYSQL_5_7. Defaults to MYSQL_5_6.
                                           PostgreSQL instances: POSTGRES_9_6 The databaseVersion property can not be changed after instance creation.
     failoverReplica              success  The name and status of the failover replica. This property is applicable only to Second Generation instances.
     complex
       available                  success  The availability status of the failover replica. A false status indicates that the failover replica is out of sync. The master can only failover to the
       boolean                             failover replica when the status is true.
       name                       success  The name of the failover replica. If specified at instance creation, a failover replica is created for the instance. The name doesn't include the project
       string                              ID. This property is applicable only to Second Generation instances.
                                           The instance type. This can be one of the following.
     instanceType                 success  " CLOUD_SQL_INSTANCE: A Cloud SQL instance that is not replicating from a master.
     string                                " ON_PREMISES_INSTANCE: An instance running on the customer's premises.
                                           " READ_REPLICA_INSTANCE: A Cloud SQL instance configured as a read-replica.
     ipAddresses                  success  The assigned IP addresses for the instance.
     complex
       ipAddress                  success  The IP address assigned.
       string
       timeToRetire               success  The due time for this IP to be retired in RFC 3339 format, for example 2012-11-15T16:19:00.094Z. This field is only available when the IP is scheduled to
       string                              be retired.
       type                       success  The type of this IP address. A PRIMARY address is an address that can accept incoming connections. An OUTGOING address is the source address of
       string                              connections originating from the instance, if supported.
     ipv6Address                  success  The IPv6 address assigned to the instance. This property is applicable only to First Generation instances.
     string
     masterInstanceName           success  The name of the instance which will act as master in the replication setup.
     string
     maxDiskSize                  success  The maximum disk size of the instance in bytes.
     integer
     name                         success  Name of the Cloud SQL instance. This does not include the project ID.
     string
     region                       success  The geographical region. Defaults to us-central or us-central1 depending on the instance type (First Generation or Second Generation/PostgreSQL).
     string
     replicaConfiguration         success  Configuration specific to failover replicas and read replicas.
     complex
       failoverTarget                      Specifies if the replica is the failover target. If the field is set to true the replica will be designated as a failover replica.
       boolean                    success  In case the master instance fails, the replica instance will be promoted as the new master instance.
                                           Only one replica can be specified as failover target, and the replica has to be in different zone with the master instance.
       mysqlReplicaConfiguration           MySQL specific configuration when replicating from a MySQL on-premises master. Replication configuration information such as the username, password,
       complex                    success  certificates, and keys are not stored in the instance metadata. The configuration information is used only to set up the replication connection and is
                                           stored by MySQL in a file named master.info in the data directory.
          caCertificate           success  PEM representation of the trusted CA's x509 certificate.
          string
          clientCertificate       success  PEM representation of the slave's x509 certificate .
          string
          clientKey               success  PEM representation of the slave's private key. The corresponding public key is encoded in the client's certificate.
          string
          connectRetryInterval    success  Seconds to wait between connect retries. MySQL's default is 60 seconds.
          integer
          dumpFilePath                     Path to a SQL dump file in Google Cloud Storage from which the slave instance is to be created. The URI is in the form gs://bucketName/fileName.
          string                  success  Compressed gzip files (.gz) are also supported. Dumps should have the binlog coordinates from which replication should begin. This can be accomplished by
                                           setting --master-data to 1 when using mysqldump.
          masterHeartbeatPeriod   success  Interval in milliseconds between replication heartbeats.
          integer
          password                success  The password for the replication connection.
          string
          sslCipher               success  A list of permissible ciphers to use for SSL encryption.
          string
          username                success  The username for the replication connection.
          string
          verifyServerCertificate success  Whether or not to check the master's Common Name value in the certificate that it sends during the SSL handshake.
          boolean
       replicaNames               success  The replicas of the instance.
       list
       serviceAccountEmailAddress success  The service account email address assigned to the instance. This property is applicable only to Second Generation instances.
       string
     settings                     success  The user settings.
     complex
       availabilityType           success  The availabilityType define if your postgres instance is run zonal or regional.
       string
       backupConfiguration        success  The daily backup configuration for the instance.
       complex
          binaryLogEnabled        success  Whether binary log is enabled. If backup configuration is disabled, binary log must be disabled as well. MySQL only.
          boolean
          enabled                 success  Enable Autobackup for your instance.
          boolean
          startTime               success  Define the backup start time in UTC (HH:MM) .
          string
       databaseFlags              success  The database flags passed to the instance at startup.
       complex
          name                    success  The name of the flag. These flags are passed at instance startup, so include both server options and system variables for MySQL. Flags should be specified
          string                           with underscores, not hyphens.
          value                   success  The value of the flag. Booleans should be set to on for true and off for false. This field must be omitted if the flag doesn't take a value.
          string
       ipConfiguration            success  The settings for IP Management. This allows to enable or disable the instance IP and manage which external networks can connect to the instance. The IPv4
       complex                             address cannot be disabled for Second Generation instances.
          authorizedNetworks      success  The list of external networks that are allowed to connect to the instance using the IP. In CIDR notation, also known as 'slash' notation (e.g.
          complex                          192.168.100.0/24).
                 expirationTime   success  The time when this access control entry expires in RFC 3339 format, for example 2012-11-15T16:19:00.094Z.
                 string
                 name             success  An optional label to identify this entry.
                 string
                 value            success  The whitelisted value for the access control list. For example, to grant access to a client from an external IP (IPv4 or IPv6) address or subnet, use that
                 string                    address or subnet here.
          ipv4Enabled             success  Whether the instance should be assigned an IP address or not.
          boolean
          requireSsl              success  Whether the mysqld should default to 'REQUIRE X509' for users connecting over IP.
          boolean
       settingsVersion            success  The version of instance settings. This is a required field for update method to make sure concurrent updates are handled properly. During update, use the
       integer                             most recent settingsVersion value for this instance and do not try to update this value.
       tier                       success  The tier or machine type for this instance, for example db-n1-standard-1. For MySQL instances, this field determines whether the instance is Second
       string                              Generation (recommended) or First Generation.

STATUS                   *gcp_sql_instance_info-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Google Inc. (@googlecloudplatform)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *gcp_sql_instance_info-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/gcp_sql_instance_info_module.html
