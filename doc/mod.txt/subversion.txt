SYNOPSIS                   *subversion*

     • Deploy given repository URL / revision to dest. If dest exists, update to the specified revision, otherwise perform a checkout.

PARAMETERS                   *subversion-parameters*

   Parameter  Choices/Defaults                                                                                Comments
   checkout     Choices:
   boolean      • no           If no, do not check out the repository if it does not exist locally.
                • yes ←
   dest
   - /                         Absolute path where the repository should be deployed.
   required
   executable                  Path to svn executable to use. If not supplied, the normal mechanism for resolving binary paths will be used.
   -
   export       Choices:
   boolean      • no ←         If yes, do export instead of checkout/update.
                • yes
   force        Choices:
   boolean      • no ←         If yes, modified files will be discarded. If no, module will fail if it encounters modified files. Prior to 1.9 the default was yes.
                • yes
   in_place     Choices:
   boolean      • no ←         If the directory exists, then the working copy will be checked-out over-the-top using svn checkout --force; if force is specified then existing files with different
   added in     • yes          content are reverted
   2.6
   password                    --password parameter passed to svn when svn is less than version 1.10.0. This is not secure and the password will be leaked to argv.
   -                           --password-from-stdin parameter when svn is greater or equal to version 1.10.0.
   repo                        The subversion URL to the repository.
   - /                         aliases: name, repository
   required
   revision   Default:         Specific revision to checkout.
   -          "HEAD"           aliases: version
   switch       Choices:
   boolean      • no           If no, do not call svn switch before update.
                • yes ←
   update       Choices:
   boolean      • no           If no, do not retrieve new revisions from the origin repository.
                • yes ←
   username                    --username parameter passed to svn.
   -

NOTES                   *subversion-notes*

   Note

     • Requires svn to be installed on the client.
     • This module does not handle externals.

EXAMPLES                   *subversion-examples*

 - name: Checkout subversion repository to specified folder
   subversion:
     repo: svn+ssh://an.example.org/path/to/repo
     dest: /src/checkout

 - name: Export subversion directory to folder
   subversion:
     repo: svn+ssh://an.example.org/path/to/repo
     dest: /src/export
     export: yes

 - name: Get information about the repository whether or not it has already been cloned locally
 - subversion:
     repo: svn+ssh://an.example.org/path/to/repo
     dest: /srv/checkout
     checkout: no
     update: no

STATUS                   *subversion-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Core Team. [core]

RED HAT SUPPORT                   *subversion-red hat support*

     • Dane Summers (@dsummersl) <njharman@gmail.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *subversion-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/subversion_module.html
