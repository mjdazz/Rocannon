SYNOPSIS                   *na_elementsw_vlan*

     • Create, delete, modify VLAN

REQUIREMENTS                   *na_elementsw_vlan-requirements*

   The below requirements are needed on the host that executes this module.

     • The modules were developed with SolidFire 10.1
     • solidfire-sdk-python (1.1.0.92) or greater. Install using ‘pip install solidfire-sdk-python’

PARAMETERS                   *na_elementsw_vlan-parameters*

     Parameter    Choices/Defaults                                                                              Comments
   address_blocks                  List of address blocks for the VLAN
   -                               Each address block contains the starting IP address and size for the block
                                   Required for create
   attributes                      Dictionary of attributes with name and value for each attribute
   -
   gateway                         Gateway for the VLAN
   -
   hostname                        The hostname or IP address of the SolidFire cluster.
   - / required
   name                            User defined name for the new VLAN
   -                               Name of the vlan is unique
                                   Required for create
   namespace        Choices:
   boolean          • no           Enable or disable namespaces
                    • yes
   netmask                         Netmask for the VLAN
   -                               Required for create
   password                        Password for the specified user.
   - / required                    aliases: pass
   state            Choices:
   -                • present ←    Whether the specified vlan should exist or not.
                    • absent
   svip                            Storage virtual IP which is unique
   -                               Required for create
   username                        Please ensure that the user has the adequate permissions. For more information, please read the official documentation
   - / required                    https://mysupport.netapp.com/documentation/docweb/index.html?productID=62636&language=en-US.
                                   aliases: user
   vlan_tag                        Virtual Network Tag
   - / required

NOTES                   *na_elementsw_vlan-notes*

   Note

     • The modules prefixed with na\_elementsw are built to support the SolidFire storage platform.

EXAMPLES                   *na_elementsw_vlan-examples*

 - name: Create vlan
   na_elementsw_vlan:
     state: present
     name: test
     vlan_tag: 1
     svip: "{{ ip address }}"
     netmask: "{{ netmask }}"
     address_blocks:
       - start: "{{ starting ip_address }}"
         size: 5
       - start: "{{ starting ip_address }}"
         size: 5
     hostname: "{{ netapp_hostname }}"
     username: "{{ netapp_username }}"
     password: "{{ netapp_password }}"

 - name: Delete Lun
   na_elementsw_vlan:
     state: absent
     vlan_tag: 1
     hostname: "{{ netapp_hostname }}"
     username: "{{ netapp_username }}"
     password: "{{ netapp_password }}"

STATUS                   *na_elementsw_vlan-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by an Ansible Partner. [certified]

     • NetApp Ansible Team (@carchi8py) <ng-ansibleteam@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_elementsw_vlan-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_elementsw_vlan_module.html
