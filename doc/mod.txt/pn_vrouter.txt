DEPRECATED                   *pn_vrouter*

                              Removed in Ansible:
                  version: 2.12
        Why:      Doesn’t support latest Pluribus Networks netvisor
    Alternative:  Latest modules will be pushed in Ansible future versions.

SYNOPSIS                   *pn_vrouter-synopsis*

     • Execute vrouter-create, vrouter-delete, vrouter-modify command.
     • Each fabric, cluster, standalone switch, or virtual network (VNET) can provide its tenants with a virtual router (vRouter) service that forwards traffic between networks and implements Layer
       3 protocols.
     • vrouter-create creates a new vRouter service.
     • vrouter-delete deletes a vRouter service.
     • vrouter-modify modifies a vRouter service.

PARAMETERS                   *pn_vrouter-parameters*

        Parameter       Choices/Defaults                                                                           Comments
   pn_bgp_as                             Specify the Autonomous System Number(ASN) if the vRouter runs Border Gateway Protocol(BGP).
   -
   pn_bgp_max_paths                      Specify the maximum number of paths for BGP. This is a number between 1 and 255 or 0 to unset.
   -
   pn_bgp_options                        Specify other BGP options as a whitespaces separated string within single quotes ''.
   -
                          Choices:
   pn_bgp_redistribute    • static
   -                      • connected    Specify how BGP routes are redistributed.
                          • rip
                          • ospf
   pn_clipassword                        Provide login password if user is not root.
   -
   pn_cliswitch         Default:         Target switch(es) to run the CLI on.
   -                    "local"
   pn_cliusername                        Provide login username if user is not root.
   -
   pn_hw_vrrp_id                         Specifies the VRRP ID for a hardware vrouter.
   -
   pn_name                               Specify the name of the vRouter.
   - / required
   pn_ospf_options                       Specify other OSPF options as a whitespaces separated string within single quotes ''.
   -
                          Choices:
   pn_ospf_redistribute   • static
   -                      • connected    Specify how OSPF routes are redistributed.
                          • bgp
                          • rip
                          Choices:
   pn_rip_redistribute    • static
   -                      • connected    Specify how RIP routes are redistributed.
                          • ospf
                          • bgp
   pn_router_id                          Specify the vRouter IP address.
   -
   pn_router_type         Choices:       Specify if the vRouter uses software or hardware.
   -                      • hardware     Note that if you specify hardware as router type, you cannot assign IP addresses using DHCP. You must specify a static IP address.
                          • software
   pn_service_state       Choices:
   -                      • enable       Specify to enable or disable vRouter service.
                          • disable
   pn_service_type        Choices:
   -                      • dedicated    Specify if the vRouter is a dedicated or shared VNET service.
                          • shared
   pn_vnet                               Specify the name of the VNET.
   -                                     Required for vrouter-create.
   pn_vrrp_track_port                    Specify list of ports and port ranges.
   -
                          Choices:
   state                  • present      State the action to perform. Use 'present' to create vrouter, 'absent' to delete vrouter and 'update' to modify vrouter.
   - / required           • absent
                          • update

EXAMPLES                   *pn_vrouter-examples*

 - name: create vrouter
   pn_vrouter:
     state: 'present'
     pn_name: 'ansible-vrouter'
     pn_vnet: 'ansible-fab-global'
     pn_router_id: 208.74.182.1

 - name: delete vrouter
   pn_vrouter:
     state: 'absent'
     pn_name: 'ansible-vrouter'

RETURN VALUES                   *pn_vrouter-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key   Returned                                                                                    Description
   changed always   Indicates whether the CLI caused changes on the target.
   boolean
   command always   The CLI command run on the target node(s).
   string
   stderr  on error The set of error responses from the vrouter command.
   list
   stdout  always   The set of responses from the vrouter command.
   list

STATUS                   *pn_vrouter-status*

     • This module will be removed in version 2.12. [deprecated]
     • For more information see DEPRECATED.

     • Pluribus Networks (@amitsi)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_vrouter-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_vrouter_module.html
