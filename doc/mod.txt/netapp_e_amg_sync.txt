SYNOPSIS                   *netapp_e_amg_sync*

     • Allows for the initialization, suspension and resumption of an asynchronous mirror group’s synchronization for NetApp E-series storage arrays.

PARAMETERS                   *netapp_e_amg_sync-parameters*

         Parameter       Choices/Defaults                                                                          Comments
   api_password                           The password to authenticate with the SANtricity WebServices Proxy or embedded REST API.
   - / required
   api_url                                The url to the SANtricity WebServices Proxy or embedded REST API.
   - / required
   api_username                           The username to authenticate with the SANtricity WebServices Proxy or embedded REST API.
   - / required
                                          Indicates whether the failures point can be deleted on the secondary if necessary to achieve the synchronization.
                           Choices:       If true, and if the amount of unsynchronized data exceeds the CoW repository capacity on the secondary for any member volume, the last failures point will
   delete_recovery_point   • no ←         be deleted and synchronization will continue.
   boolean                 • yes          If false, the synchronization will be suspended if the amount of unsynchronized data exceeds the CoW Repository capacity on the secondary and the failures
                                          point will be preserved.
                                          NOTE: This only has impact for newly launched syncs.
   name                                   The name of the async mirror group you wish to target
   - / required
   ssid                                   The ID of the storage array containing the AMG you wish to target
   -
                           Choices:       The synchronization action you'd like to take.
   state                   • running      If running then it will begin syncing if there is no active sync or will resume a suspended sync. If there is already a sync in progress, it will return
   - / required            • suspended    with an OK status.
                                          If suspended it will suspend any ongoing sync action, but return OK if there is no active sync or if the sync is already suspended
   validate_certs          Choices:
   boolean                 • no           Should https certificates be validated?
                           • yes ←

EXAMPLES                   *netapp_e_amg_sync-examples*

 - name: start AMG async
   netapp_e_amg_sync:
     name: "{{ amg_sync_name }}"
     state: running
     ssid: "{{ ssid }}"
     api_url: "{{ netapp_api_url }}"
     api_username: "{{ netapp_api_username }}"
     api_password: "{{ netapp_api_password }}"

RETURN VALUES                   *netapp_e_amg_sync-return values*

   Common return values are documented here, the following are the fields unique to this module:

    Key   Returned                                                                                    Description
   json   success  The object attributes of the AMG.
   string

STATUS                   *netapp_e_amg_sync-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Kevin Hulquest (@hulquest)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *netapp_e_amg_sync-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/netapp_e_amg_sync_module.html
