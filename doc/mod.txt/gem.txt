SYNOPSIS                   *gem*

     • Manage installation and uninstallation of Ruby gems.

PARAMETERS                   *gem-parameters*

        Parameter       Choices/Defaults                                                                           Comments
   build_flags                           Allow adding build flags for gem compilation
   -
   env_shebang            Choices:
   boolean                • no ←         Rewrite the shebang line on installed scripts to use /usr/bin/env.
                          • yes
   executable                            Override the path to the gem executable
   -
   force                  Choices:
   boolean                • no ←         Force gem to install, bypassing dependency checks.
   added in 2.8           • yes
   gem_source                            The path to a local gem used as installation source.
   -
   include_dependencies   Choices:
   boolean                • no           Whether to include dependencies or not.
                          • yes ←
   include_doc            Choices:
   boolean                • no ←         Install with or without docs.
                          • yes
   install_dir                           Install the gems into a specific directory. These gems will be independent from the global installed ones. Specifying this requires user_install to be
   -                                     false.
   added in 2.6
   name                                  The name of the gem to be managed.
   - / required
   pre_release            Choices:
   boolean                • no ←         Allow installation of pre-release versions of the gem.
                          • yes
   repository                            The repository from which the gem will be installed
   -                                     aliases: source
                          Choices:
   state                  • present ←    The desired state of the gem. latest ensures that the latest version is installed.
   -                      • absent
                          • latest
   user_install           Choices:
   boolean                • no           Install gem in user's local gems cache or for all users
                          • yes ←
   version                               Version of the gem to be installed/removed.
   -

EXAMPLES                   *gem-examples*

 # Installs version 1.0 of vagrant.
 - gem:
     name: vagrant
     version: 1.0
     state: present

 # Installs latest available version of rake.
 - gem:
     name: rake
     state: latest

 # Installs rake version 1.0 from a local gem on disk.
 - gem:
     name: rake
     gem_source: /path/to/gems/rake-1.0.gem
     state: present

STATUS                   *gem-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Ansible Core Team
     • Johan Wiren (@johanwiren)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *gem-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/gem_module.html
