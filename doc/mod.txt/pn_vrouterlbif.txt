DEPRECATED                   *pn_vrouterlbif*

                              Removed in Ansible:
                  version: 2.12
        Why:      Doesn’t support latest Pluribus Networks netvisor
    Alternative:  Latest modules will be pushed in Ansible future versions.

SYNOPSIS                   *pn_vrouterlbif-synopsis*

     • Execute vrouter-loopback-interface-add, vrouter-loopback-interface-remove commands.
     • Each fabric, cluster, standalone switch, or virtual network (VNET) can provide its tenants with a virtual router (vRouter) service that forwards traffic between networks and implements Layer
       3 protocols.

PARAMETERS                   *pn_vrouterlbif-parameters*

      Parameter    Choices/Defaults                                                                             Comments
   pn_clipassword                   Provide login password if user is not root.
   -
   pn_cliswitch    Default:         Target switch(es) to run the cli on.
   -               "local"
   pn_cliusername                   Provide login username if user is not root.
   -
   pn_index                         Specify the interface index from 1 to 255.
   -
   pn_interface_ip                  Specify the IP address.
   - / required
   pn_vrouter_name                  Specify the name of the vRouter.
   - / required
   state             Choices:
   - / required      • present      State the action to perform. Use 'present' to add vrouter loopback interface and 'absent' to remove vrouter loopback interface.
                     • absent

EXAMPLES                   *pn_vrouterlbif-examples*

 - name: add vrouter-loopback-interface
   pn_vrouterlbif:
     state: 'present'
     pn_vrouter_name: 'ansible-vrouter'
     pn_interface_ip: '104.104.104.1'

 - name: remove vrouter-loopback-interface
   pn_vrouterlbif:
     state: 'absent'
     pn_vrouter_name: 'ansible-vrouter'
     pn_interface_ip: '104.104.104.1'

RETURN VALUES                   *pn_vrouterlbif-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key   Returned                                                                                    Description
   changed always   Indicates whether the CLI caused changes on the target.
   boolean
   command always   The CLI command run on the target node(s).
   string
   stderr  on error The set of error responses from the vrouterlb command.
   list
   stdout  always   The set of responses from the vrouterlb command.
   list

STATUS                   *pn_vrouterlbif-status*

     • This module will be removed in version 2.12. [deprecated]
     • For more information see DEPRECATED.

     • Pluribus Networks (@amitsi)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_vrouterlbif-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_vrouterlbif_module.html
