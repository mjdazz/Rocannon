SYNOPSIS                   *apache2_module*

     • Enables or disables a specified module of the Apache2 webserver.

REQUIREMENTS                   *apache2_module-requirements*

   The below requirements are needed on the host that executes this module.

     • a2enmod
     • a2dismod

PARAMETERS                   *apache2_module-parameters*

       Parameter      Choices/Defaults                                                                            Comments
   force                Choices:
   boolean              • no ←         Force disabling of default modules and override Debian warnings.
                        • yes
   identifier                          Identifier of the module as listed by apache2ctl -M. This is optional and usually determined automatically by the common convention of appending _module to
   -                                   name as well as custom exception for popular modules.
   added in 2.5
   ignore_configcheck   Choices:
   boolean              • no ←         Ignore configuration checks about inconsistent module configuration. Especially for mpm_* modules.
                        • yes
   name                                Name of the module to enable/disable as given to a2enmod/a2dismod.
   - / required
   state                Choices:
   -                    • present ←    Desired state of the module.
                        • absent

Examples¶

 # enables the Apache2 module "wsgi"
 - apache2_module:
     state: present
     name: wsgi
 # disables the Apache2 module "wsgi"
 - apache2_module:
     state: absent
     name: wsgi
 # disable default modules for Debian
 - apache2_module:
     state: absent
     name: autoindex
     force: True
 # disable mpm_worker and ignore warnings about missing mpm module
 - apache2_module:
     state: absent
     name: mpm_worker
     ignore_configcheck: True
 # enable dump_io module, which is identified as dumpio_module inside apache2
 - apache2_module:
     state: present
     name: dump_io
     identifier: dumpio_module

Return Values¶

   Common return values are documented here, the following are the fields unique to this module:

     Key     Returned                                                                                    Description
   rc       failed      return code of underlying command
   integer
   result   always      message about action taken
   string
   stderr   failed      stderr of underlying command
   string
   stdout   failed      stdout of underlying command
   string
   warnings when needed list of warning messages
   list

Status¶

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

  Authors¶

     • Christian Berendt (@berendt)
     • Ralf Hertel (@n0trax)
     • Robin Roth (@robinro)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

NOTES                   *apache2_module-notes*

EXAMPLES                   *apache2_module-examples*

 # enables the Apache2 module "wsgi"
 - apache2_module:
     state: present
     name: wsgi
 # disables the Apache2 module "wsgi"
 - apache2_module:
     state: absent
     name: wsgi
 # disable default modules for Debian
 - apache2_module:
     state: absent
     name: autoindex
     force: True
 # disable mpm_worker and ignore warnings about missing mpm module
 - apache2_module:
     state: absent
     name: mpm_worker
     ignore_configcheck: True
 # enable dump_io module, which is identified as dumpio_module inside apache2
 - apache2_module:
     state: present
     name: dump_io
     identifier: dumpio_module

RETURN VALUES                   *apache2_module-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key     Returned                                                                                    Description
   rc       failed      return code of underlying command
   integer
   result   always      message about action taken
   string
   stderr   failed      stderr of underlying command
   string
   stdout   failed      stdout of underlying command
   string
   warnings when needed list of warning messages
   list

STATUS                   *apache2_module-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Christian Berendt (@berendt)
     • Ralf Hertel (@n0trax)
     • Robin Roth (@robinro)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *apache2_module-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/apache2_module_module.html
