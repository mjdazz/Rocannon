SYNOPSIS                   *frr_facts*

     • Collects a base set of device facts from a remote device that is running FRR. This module prepends all of the base network fact keys with ansible_net_<fact>. The facts module will always
       collect a base set of facts from the device and can enable or disable collection of additional facts.

PARAMETERS                   *frr_facts-parameters*

     Parameter   Choices/Defaults                                                                              Comments
                                  When supplied, this argument restricts the facts collected to a given subset.
   gather_subset Default:         Possible values for this argument include all, hardware, config, and interfaces.
   -             "!config"        Specify a list of values to include a larger subset.
                                  Use a value with an initial ! to collect all facts except that subset.

NOTES                   *frr_facts-notes*

   Note

     • Tested against FRR 6.0.

EXAMPLES                   *frr_facts-examples*

 - name: Collect all facts from the device
   frr_facts:
     gather_subset: all

 - name: Collect only the config and default facts
   frr_facts:
     gather_subset:
       - config

 - name: Collect the config and hardware facts
   frr_facts:
     gather_subset:
       - config
       - hardware

 - name: Do not collect hardware facts
   frr_facts:
     gather_subset:
       - "!hardware"

RETURN VALUES                   *frr_facts-return values*

   Common return values are documented here, the following are the fields unique to this module:

                Key                                             Returned                                                                         Description
   ansible_net_all_ipv4_addresses when interfaces is configured                                         All IPv4 addresses configured on the device
   list
   ansible_net_all_ipv6_addresses when interfaces is configured                                         All IPv6 addresses configured on the device
   list
   ansible_net_api                always                                                                The name of the transport
   string
   ansible_net_config             when config is configured                                             The current active config from the device
   string
   ansible_net_gather_subset      always                                                                The list of fact subsets collected from the device
   list
   ansible_net_hostname           always                                                                The configured hostname of the device
   string
   ansible_net_interfaces         when interfaces is configured                                         A hash of all interfaces running on the system
   dictionary
   ansible_net_mem_stats          when hardware is configured                                           The memory statistics fetched from the device
   dictionary
   ansible_net_mpls_ldp_neighbors when interfaces is configured and LDP daemon is running on the device The list of MPLS LDP neighbors from the remote device
   dictionary
   ansible_net_python_version     always                                                                The Python version that the Ansible controller is using
   string
   ansible_net_version            always                                                                The FRR version running on the remote device
   string

STATUS                   *frr_facts-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Network Team. [network]

RED HAT SUPPORT                   *frr_facts-red hat support*

     • Nilashish Chakraborty (@NilashishC)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *frr_facts-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/frr_facts_module.html
