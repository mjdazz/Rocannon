SYNOPSIS                   *utm_aaa_group*

     • Create, update or destroy an aaa group object in Sophos UTM.
     • This module needs to have the REST Ability of the UTM to be activated.

PARAMETERS                   *utm_aaa_group-parameters*

         Parameter          Choices/Defaults                                                                          Comments
   adirectory_groups                           List of adirectory group strings.
   list
   adirectory_groups_sids                      Dictionary of group sids.
   dictionary
                            Choices:
                            • none ←
   backend_match            • adirectory
   string                   • edirectory       The backend for the group.
                            • radius
                            • tacacs
                            • ldap
   comment                Default:             Comment that describes the AAA group.
   string                 ""
                            Choices:
   dynamic                  • none ←           Group type. Is static if none is selected.
   string                   • ipsec_dn
                            • directory_groups
   edirectory_groups                           List of edirectory group strings.
   list
   headers                                     A dictionary of additional headers to be sent to POST and PUT requests.
   dictionary                                  Is needed for some modules
   ipsec_dn                                    The ipsec dn string.
   string
   ldap_attribute                              The ldap attribute to check against.
   string
   ldap_attribute_value                        The ldap attribute value to check against.
   string
   members                Default:             A list of user ref names (aaa/user).
   list                   []
   name                                        The name of the object. Will be used to identify the entry.
   string / required
   network                Default:             The network reference name. The objects contains the known ip addresses for the authentication object (network/aaa).
   string                 ""
   radius_groups          Default:             A list of radius group strings.
   list                   []
   state                    Choices:           The desired state of the object.
   string                   • absent           present will create or update an object
                            • present ←        absent will delete an object if it was present
   tacacs_groups          Default:             A list of tacacs group strings.
   list                   []
   utm_host                                    The REST Endpoint of the Sophos UTM.
   string / required
   utm_port               Default:             The port of the REST interface.
   integer                4444
   utm_protocol             Choices:
   string                   • http             The protocol of the REST Endpoint.
                            • https ←
   utm_token                                   The token used to identify at the REST-API. See
   string / required                           https://www.sophos.com/en-us/medialibrary/PDFs/documentation/UTMonAWS/Sophos-UTM-RESTful-API.pdf?la=en, Chapter 2.4.2.
   validate_certs           Choices:
   boolean                  • no               Whether the REST interface's ssl certificate should be verified or not.
                            • yes ←

EXAMPLES                   *utm_aaa_group-examples*

 - name: Create UTM aaa_group
   utm_aaa_group:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestAAAGroupEntry
     backend_match: ldap
     dynamic: directory_groups
     ldap_attributes: memberof
     ldap_attributes_value: "cn=groupname,ou=Groups,dc=mydomain,dc=com"
     network: REF_OBJECT_STRING
     state: present

 - name: Remove UTM aaa_group
   utm_aaa_group:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestAAAGroupEntry
     state: absent

RETURN VALUES                   *utm_aaa_group-return values*

   Common return values are documented here, the following are the fields unique to this module:

             Key            Returned                                                                           Description
   result                   success  The utm object that was created.
   complex
     _locked                         Whether or not the object is currently locked.
     boolean
     _ref                            The reference name of the object.
     string
     _type                           The type of the object.
     string
     adirectory_groups               List of Active Directory Groups.
     string
     adirectory_groups_sids          List of Active Directory Groups SIDS.
     list
     backend_match                   The backend to use.
     string
     comment                         The comment string.
     string
     dynamic                         Whether the group match is ipsec_dn or directory_group.
     string
     edirectory_groups               List of eDirectory Groups.
     string
     ipsec_dn                        ipsec_dn identifier to match.
     string
     ldap_attribute                  The LDAP Attribute to match against.
     string
     ldap_attribute_value            The LDAP Attribute Value to match against.
     string
     members                         List of member identifiers of the group.
     list
     name                            The name of the object.
     string
     network                         The identifier of the network (network/aaa).
     string
     radius_group                    The radius group identifier.
     string
     tacacs_group                    The tacacs group identifier.
     string

STATUS                   *utm_aaa_group-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Johannes Brunswicker (@MatrixCrawler)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *utm_aaa_group-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/utm_aaa_group_module.html
