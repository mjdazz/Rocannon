SYNOPSIS                   *utm_proxy_auth_profile*

     • Create, update or destroy a reverse_proxy auth_profile entry in SOPHOS UTM.
     • This module needs to have the REST Ability of the UTM to be activated.

PARAMETERS                   *utm_proxy_auth_profile-parameters*

               Parameter              Choices/Defaults                                                                    Comments
   aaa                                                 List of references to utm_aaa objects (allowed users or groups)
   - / required
   backend_mode                         Choices:
   -                                    • Basic        Specifies if the backend server needs authentication ([Basic|None])
                                        • None ←
   backend_strip_basic_auth             Choices:
   boolean                              • no           Should the login data be stripped when proxying the request to the backend host
                                        • yes ←
   backend_user_prefix                Default:         Prefix string to prepend to the username for backend authentication
   -                                  ""
   backend_user_suffix                Default:         Suffix string to append to the username for backend authentication
   -                                  ""
   basic_prompt                                        The message in the basic authentication prompt
   - / required
   comment                            Default:         Optional comment string
   -                                  ""
   frontend_cookie                                     Frontend cookie name
   -
   frontend_cookie_secret                              Frontend cookie secret
   -
   frontend_form                                       Frontend authentication form name
   -
   frontend_form_template             Default:         Frontend authentication form template
   -                                  ""
   frontend_login                                      Frontend login name
   -
   frontend_logout                                     Frontend logout name
   -
   frontend_mode                        Choices:
   -                                    • Basic ←      Frontend authentication mode (Form|Basic)
                                        • Form
   frontend_realm                                      Frontend authentication realm
   -
   frontend_session_allow_persistency   Choices:
   boolean                              • no ←         Allow session persistency
                                        • yes
   frontend_session_lifetime                           session lifetime
   - / required
   frontend_session_lifetime_limited    Choices:
   boolean                              • no           Specifies if limitation of session lifetime is active
                                        • yes ←
                                        Choices:
   frontend_session_lifetime_scope      • days         scope for frontend_session_lifetime (days|hours|minutes)
   -                                    • hours ←
                                        • minutes
   frontend_session_timeout                            session timeout
   - / required
   frontend_session_timeout_enabled     Choices:
   boolean                              • no           Specifies if session timeout is active
                                        • yes ←
                                        Choices:
   frontend_session_timeout_scope       • days         scope for frontend_session_timeout (days|hours|minutes)
   -                                    • hours
                                        • minutes ←
   headers                                             A dictionary of additional headers to be sent to POST and PUT requests.
   dictionary                                          Is needed for some modules
   logout_delegation_urls             Default:         List of logout URLs that logouts are delegated to
   -                                  []
   logout_mode                          Choices:
   -                                    • None ←       Mode of logout (None|Delegation)
                                        • Delegation
   name                                                The name of the object. Will be used to identify the entry
   - / required
   redirect_to_requested_url            Choices:
   boolean                              • no ←         Should a redirect to the requested URL be made
                                        • yes
   state                                Choices:       The desired state of the object.
   string                               • absent       present will create or update an object
                                        • present ←    absent will delete an object if it was present
   utm_host                                            The REST Endpoint of the Sophos UTM.
   string / required
   utm_port                           Default:         The port of the REST interface.
   integer                            4444
   utm_protocol                         Choices:
   string                               • http         The protocol of the REST Endpoint.
                                        • https ←
   utm_token                                           The token used to identify at the REST-API. See
   string / required                                   https://www.sophos.com/en-us/medialibrary/PDFs/documentation/UTMonAWS/Sophos-UTM-RESTful-API.pdf?la=en, Chapter 2.4.2.
   validate_certs                       Choices:
   boolean                              • no           Whether the REST interface's ssl certificate should be verified or not.
                                        • yes ←

EXAMPLES                   *utm_proxy_auth_profile-examples*

 - name: Create UTM proxy_auth_profile
   utm_proxy_auth_profile:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestAuthProfileEntry
     aaa: [REF_OBJECT_STRING,REF_ANOTHEROBJECT_STRING]
     basic_prompt: "Authentication required: Please login"
     frontend_session_lifetime: 1
     frontend_session_timeout: 1
     state: present

 - name: Remove UTM proxy_auth_profile
   utm_proxy_auth_profile:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestAuthProfileEntry
     state: absent

 - name: Read UTM proxy_auth_profile
   utm_proxy_auth_profile:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestAuthProfileEntry
     state: info

RETURN VALUES                   *utm_proxy_auth_profile-return values*

   Common return values are documented here, the following are the fields unique to this module:

                   Key                  Returned                                                                     Description
   result                               success  The utm object that was created
   complex
     _locked                                     Whether or not the object is currently locked
     boolean
     _ref                                        The reference name of the object
     string
     _type                                       The type of the object
     string
     aaa                                         List of references to utm_aaa objects (allowed users or groups)
     list
     backend_mode                                Specifies if the backend server needs authentication ([Basic|None])
     string
     backend_strip_basic_auth                    Should the login data be stripped when proxying the request to the backend host
     boolean
     backend_user_prefix                         Prefix string to prepend to the username for backend authentication
     string
     backend_user_suffix                         Suffix string to append to the username for backend authentication
     string
     basic_prompt                                The message in the basic authentication prompt
     string
     comment                                     Optional comment string
     string
     frontend_cookie                             Frontend cookie name
     string
     frontend_cookie_secret                      Frontend cookie secret
     string
     frontend_form                               Frontend authentication form name
     string
     frontend_form_template                      Frontend authentication form template
     string
     frontend_login                              Frontend login name
     string
     frontend_logout                             Frontend logout name
     string
     frontend_mode                               Frontend authentication mode (Form|Basic)
     string
     frontend_realm                              Frontend authentication realm
     string
     frontend_session_allow_persistency          Allow session persistency
     boolean
     frontend_session_lifetime                   session lifetime
     integer
     frontend_session_lifetime_limited           Specifies if limitation of session lifetime is active
     boolean
     frontend_session_lifetime_scope             scope for frontend_session_lifetime (days|hours|minutes)
     string
     frontend_session_timeout                    session timeout
     integer
     frontend_session_timeout_enabled            Specifies if session timeout is active
     boolean
     frontend_session_timeout_scope              scope for frontend_session_timeout (days|hours|minutes)
     string
     logout_delegation_urls                      List of logout URLs that logouts are delegated to
     list
     logout_mode                                 Mode of logout (None|Delegation)
     string
     name                                        The name of the object
     string
     redirect_to_requested_url                   Should a redirect to the requested URL be made
     boolean

STATUS                   *utm_proxy_auth_profile-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Stephan Schwarz (@stearz)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *utm_proxy_auth_profile-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/utm_proxy_auth_profile_module.html
