SYNOPSIS                   *fmgr_secprof_wanopt*

     • Manage WanOpt security profiles in FortiManager via API

PARAMETERS                   *fmgr_secprof_wanopt-parameters*

           Parameter          Choices/Defaults                                                                        Comments
   adom                      Default:           The ADOM the configuration should belong to.
   -                         "root"
   auth_group                                   Optionally add an authentication group to restrict access to the WAN Optimization tunnel to peers in the authentication group.
   -
                                                EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                List of multiple child objects to be added. Expects a list of dictionaries.
   cifs                                         Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                            If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                This object is MUTUALLY EXCLUSIVE with its options.
                                                We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   cifs_byte_caching           Choices:         Enable/disable byte-caching for HTTP. Byte caching reduces the amount of traffic by caching file data sent across the WAN and in future serving if
   -                           • disable        from the cache.
                               • enable
   cifs_log_traffic            Choices:
   -                           • disable        Enable/disable logging.
                               • enable
   cifs_port                                    Single port number or port number range for CIFS. Only packets with a destination port number that matches this port number or range are accepted by
   -                                            this profile.
   cifs_prefer_chunking        Choices:
   -                           • dynamic        Select dynamic or fixed-size data chunking for HTTP WAN Optimization.
                               • fix
   cifs_secure_tunnel          Choices:
   -                           • disable        Enable/disable securing the WAN Opt tunnel using SSL. Secure and non-secure tunnels use the same TCP port (7810).
                               • enable
   cifs_status                 Choices:
   -                           • disable        Enable/disable HTTP WAN Optimization.
                               • enable
                               Choices:
   cifs_tunnel_sharing         • private        Tunnel sharing mode for aggressive/non-aggressive and/or interactive/non-interactive protocols.
   -                           • shared
                               • express-shared
   comments                                     Comment.
   -
                                                EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                List of multiple child objects to be added. Expects a list of dictionaries.
   ftp                                          Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                            If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                This object is MUTUALLY EXCLUSIVE with its options.
                                                We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   ftp_byte_caching            Choices:         Enable/disable byte-caching for HTTP. Byte caching reduces the amount of traffic by caching file data sent across the WAN and in future serving if
   -                           • disable        from the cache.
                               • enable
   ftp_log_traffic             Choices:
   -                           • disable        Enable/disable logging.
                               • enable
   ftp_port                                     Single port number or port number range for FTP. Only packets with a destination port number that matches this port number or range are accepted by
   -                                            this profile.
   ftp_prefer_chunking         Choices:
   -                           • dynamic        Select dynamic or fixed-size data chunking for HTTP WAN Optimization.
                               • fix
   ftp_secure_tunnel           Choices:
   -                           • disable        Enable/disable securing the WAN Opt tunnel using SSL. Secure and non-secure tunnels use the same TCP port (7810).
                               • enable
   ftp_status                  Choices:
   -                           • disable        Enable/disable HTTP WAN Optimization.
                               • enable
                               Choices:
   ftp_tunnel_sharing          • private        Tunnel sharing mode for aggressive/non-aggressive and/or interactive/non-interactive protocols.
   -                           • shared
                               • express-shared
                                                EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                List of multiple child objects to be added. Expects a list of dictionaries.
   http                                         Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                            If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                This object is MUTUALLY EXCLUSIVE with its options.
                                                We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   http_byte_caching           Choices:         Enable/disable byte-caching for HTTP. Byte caching reduces the amount of traffic by caching file data sent across the WAN and in future serving if
   -                           • disable        from the cache.
                               • enable
   http_log_traffic            Choices:
   -                           • disable        Enable/disable logging.
                               • enable
   http_port                                    Single port number or port number range for HTTP. Only packets with a destination port number that matches this port number or range are accepted by
   -                                            this profile.
   http_prefer_chunking        Choices:
   -                           • dynamic        Select dynamic or fixed-size data chunking for HTTP WAN Optimization.
                               • fix
   http_secure_tunnel          Choices:
   -                           • disable        Enable/disable securing the WAN Opt tunnel using SSL. Secure and non-secure tunnels use the same TCP port (7810).
                               • enable
   http_ssl                    Choices:
   -                           • disable        Enable/disable SSL/TLS offloading (hardware acceleration) for HTTPS traffic in this tunnel.
                               • enable
   http_ssl_port                                Port on which to expect HTTPS traffic for SSL/TLS offloading.
   -
   http_status                 Choices:
   -                           • disable        Enable/disable HTTP WAN Optimization.
                               • enable
   http_tunnel_non_http        Choices:         Configure how to process non-HTTP traffic when a profile configured for HTTP traffic accepts a non-HTTP session. Can occur if an application sends
   -                           • disable        non-HTTP traffic using an HTTP destination port.
                               • enable
                               Choices:
   http_tunnel_sharing         • private        Tunnel sharing mode for aggressive/non-aggressive and/or interactive/non-interactive protocols.
   -                           • shared
                               • express-shared
                               Choices:
   http_unknown_http_version   • best-effort    How to handle HTTP sessions that do not comply with HTTP 0.9, 1.0, or 1.1.
   -                           • reject
                               • tunnel
                                                EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                List of multiple child objects to be added. Expects a list of dictionaries.
   mapi                                         Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                            If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                This object is MUTUALLY EXCLUSIVE with its options.
                                                We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   mapi_byte_caching           Choices:         Enable/disable byte-caching for HTTP. Byte caching reduces the amount of traffic by caching file data sent across the WAN and in future serving if
   -                           • disable        from the cache.
                               • enable
   mapi_log_traffic            Choices:
   -                           • disable        Enable/disable logging.
                               • enable
   mapi_port                                    Single port number or port number range for MAPI. Only packets with a destination port number that matches this port number or range are accepted by
   -                                            this profile.
   mapi_secure_tunnel          Choices:
   -                           • disable        Enable/disable securing the WAN Opt tunnel using SSL. Secure and non-secure tunnels use the same TCP port (7810).
                               • enable
   mapi_status                 Choices:
   -                           • disable        Enable/disable HTTP WAN Optimization.
                               • enable
                               Choices:
   mapi_tunnel_sharing         • private        Tunnel sharing mode for aggressive/non-aggressive and/or interactive/non-interactive protocols.
   -                           • shared
                               • express-shared
                               Choices:
   mode                        • add ←          Sets one of three modes for managing the object.
   -                           • set            Allows use of soft-adds instead of overwriting existing values
                               • delete
                               • update
   name                                         Profile name.
   -
                                                EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                List of multiple child objects to be added. Expects a list of dictionaries.
   tcp                                          Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                            If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                This object is MUTUALLY EXCLUSIVE with its options.
                                                We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   tcp_byte_caching            Choices:         Enable/disable byte-caching for HTTP. Byte caching reduces the amount of traffic by caching file data sent across the WAN and in future serving if
   -                           • disable        from the cache.
                               • enable
   tcp_byte_caching_opt        Choices:
   -                           • mem-only       Select whether TCP byte-caching uses system memory only or both memory and disk space.
                               • mem-disk
   tcp_log_traffic             Choices:
   -                           • disable        Enable/disable logging.
                               • enable
   tcp_port                                     Single port number or port number range for TCP. Only packets with a destination port number that matches this port number or range are accepted by
   -                                            this profile.
   tcp_secure_tunnel           Choices:
   -                           • disable        Enable/disable securing the WAN Opt tunnel using SSL. Secure and non-secure tunnels use the same TCP port (7810).
                               • enable
   tcp_ssl                     Choices:
   -                           • disable        Enable/disable SSL/TLS offloading.
                               • enable
   tcp_ssl_port                                 Port on which to expect HTTPS traffic for SSL/TLS offloading.
   -
   tcp_status                  Choices:
   -                           • disable        Enable/disable HTTP WAN Optimization.
                               • enable
                               Choices:
   tcp_tunnel_sharing          • private        Tunnel sharing mode for aggressive/non-aggressive and/or interactive/non-interactive protocols.
   -                           • shared
                               • express-shared
   transparent                 Choices:
   -                           • disable        Enable/disable transparent mode.
                               • enable

NOTES                   *fmgr_secprof_wanopt-notes*

   Note

     • Full Documentation at https://ftnt-ansible-docs.readthedocs.io/en/latest/.

EXAMPLES                   *fmgr_secprof_wanopt-examples*

 - name: DELETE Profile
   fmgr_secprof_wanopt:
     name: "Ansible_WanOpt_Profile"
     mode: "delete"

 - name: Create FMGR_WANOPT_PROFILE
   fmgr_secprof_wanopt:
     mode: "set"
     adom: "root"
     transparent: "enable"
     name: "Ansible_WanOpt_Profile"
     comments: "Created by Ansible"
     cifs: {byte-caching: "enable",
             log-traffic: "enable",
             port: 80,
             prefer-chunking: "dynamic",
             status: "enable",
             tunnel-sharing: "private"}
     ftp: {byte-caching: "enable",
             log-traffic: "enable",
             port: 80,
             prefer-chunking: "dynamic",
             secure-tunnel: "disable",
             status: "enable",
             tunnel-sharing: "private"}

RETURN VALUES                   *fmgr_secprof_wanopt-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   api_result always   full API response, includes status code and message
   string

STATUS                   *fmgr_secprof_wanopt-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Luke Weighall (@lweighall)
     • Andrew Welsh (@Ghilli3)
     • Jim Huber (@p4r4n0y1ng)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *fmgr_secprof_wanopt-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/fmgr_secprof_wanopt_module.html
