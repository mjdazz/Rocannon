SYNOPSIS                   *scaleway_volume*

     • This module manages volumes on Scaleway account https://developer.scaleway.com

PARAMETERS                   *scaleway_volume-parameters*

      Parameter          Choices/Defaults                                                                             Comments
   api_timeout      Default:                   HTTP timeout to Scaleway API in seconds.
   integer          30                         aliases: timeout
   api_token                                   Scaleway OAuth token.
   string                                      aliases: oauth_token
   api_url          Default:                   Scaleway API URL.
   string           "https://api.scaleway.com" aliases: base_url
   name                                        Name used to identify the volume.
   - / required
   organization                                ScaleWay organization ID to which volume belongs.
   -
   query_parameters Default:                   List of parameters passed to the query string.
   dictionary       {}
                      Choices:
   region             • ams1
   - / required       • EMEA-NL-EVS            Scaleway region to use (for example par1).
                      • par1
                      • EMEA-FR-PAR1
   size                                        Size of the volume in bytes.
   -
   state              Choices:
   -                  • present ←              Indicate desired state of the volume.
                      • absent
   validate_certs     Choices:
   boolean            • no                     Validate SSL certs of the Scaleway API.
                      • yes ←
   volume_type                                 Type of the volume (for example 'l_ssd').
   -

NOTES                   *scaleway_volume-notes*

   Note

     • Also see the API documentation on https://developer.scaleway.com/
     • If api_token is not set within the module, the following environment variables can be used in decreasing order of precedence SCW_TOKEN, SCW_API_KEY, SCW_OAUTH_TOKEN or SCW_API_TOKEN.
     • If one wants to use a different api_url one can also set the SCW_API_URL environment variable.

EXAMPLES                   *scaleway_volume-examples*

 - name: Create 10GB volume
   scaleway_volume:
     name: my-volume
     state: present
     region: par1
     organization: "{{ scw_org }}"
     "size": 10000000000
     volume_type: l_ssd
   register: server_creation_check_task

 - name: Make sure volume deleted
   scaleway_volume:
     name: my-volume
     state: absent
     region: par1

RETURN VALUES                   *scaleway_volume-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key       Returned                                                                                   Description
                            This is only present when state=present
   data       when          Sample:
   dictionary state=present {'volume': {'export_uri': None, 'id': 'c675f420-cfeb-48ff-ba2a-9d2a4dbe3fcd', 'name': 'volume-0-3', 'organization': '000a115d-2852-4b0a-9ce8-47f1134ba95a', 'server':
                            None, 'size': 10000000000, 'volume_type': 'l_ssd'}}

STATUS                   *scaleway_volume-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Henryk Konsek (@hekonsek)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *scaleway_volume-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/scaleway_volume_module.html
