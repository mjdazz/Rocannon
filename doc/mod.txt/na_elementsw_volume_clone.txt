SYNOPSIS                   *na_elementsw_volume_clone*

     • Create volume clones on Element OS

REQUIREMENTS                   *na_elementsw_volume_clone-requirements*

   The below requirements are needed on the host that executes this module.

     • The modules were developed with SolidFire 10.1
     • solidfire-sdk-python (1.1.0.92) or greater. Install using ‘pip install solidfire-sdk-python’

PARAMETERS                   *na_elementsw_volume_clone-parameters*

      Parameter      Choices/Defaults                                                                              Comments
                     Choices:            Access allowed for the volume.
                     • readOnly          If unspecified, the access settings of the clone will be the same as the source.
   access            • readWrite         readOnly - Only read operations are allowed.
   -                 • locked            readWrite - Reads and writes are allowed.
                     • replicationTarget locked - No reads or writes are allowed.
                                         replicationTarget - Identify a volume as the target volume for a paired set of volumes. If the volume is not paired, the access status is locked.
   account_id                            Account ID for the owner of this cloned volume. id may be a numeric identifier or an account name.
   - / required
   attributes                            A YAML dictionary of attributes that you would like to apply on this cloned volume.
   -
   hostname                              The hostname or IP address of the SolidFire cluster.
   - / required
   name                                  The name of the clone.
   - / required
   password                              Password for the specified user.
   - / required                          aliases: pass
   size                                  The size of the cloned volume in (size_unit).
   -
                     Choices:
                     • bytes
                     • b
                     • kb
   size_unit         • mb
   -                 • gb ←              The unit used to interpret the size parameter.
                     • tb
                     • pb
                     • eb
                     • zb
                     • yb
   src_snapshot_id                       The id of the snapshot to clone. id may be a numeric identifier or a snapshot name.
   -
   src_volume_id                         The id of the src volume to clone. id may be a numeric identifier or a volume name.
   - / required
   username                              Please ensure that the user has the adequate permissions. For more information, please read the official documentation
   - / required                          https://mysupport.netapp.com/documentation/docweb/index.html?productID=62636&language=en-US.
                                         aliases: user

NOTES                   *na_elementsw_volume_clone-notes*

   Note

     • The modules prefixed with na\_elementsw are built to support the SolidFire storage platform.

EXAMPLES                   *na_elementsw_volume_clone-examples*

 - name: Clone Volume
   na_elementsw_volume_clone:
     hostname: "{{ elementsw_hostname }}"
     username: "{{ elementsw_username }}"
     password: "{{ elementsw_password }}"
     name: CloneAnsibleVol
     src_volume_id: 123
     src_snapshot_id: 41
     account_id: 3
     size: 1
     size_unit: gb
     access: readWrite
     attributes: {"virtual_network_id": 12345}

RETURN VALUES                   *na_elementsw_volume_clone-return values*

   Common return values are documented here, the following are the fields unique to this module:

    Key   Returned                                                                                    Description
   msg    success  Success message
   string

STATUS                   *na_elementsw_volume_clone-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by an Ansible Partner. [certified]

     • NetApp Ansible Team (@carchi8py) <ng-ansibleteam@netapp.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *na_elementsw_volume_clone-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/na_elementsw_volume_clone_module.html
