DEPRECATED                   *pn_trunk*

                              Removed in Ansible:
                  version: 2.12
        Why:      Doesn’t support latest Pluribus Networks netvisor
    Alternative:  Latest modules will be pushed in Ansible future versions.

SYNOPSIS                   *pn_trunk-synopsis*

     • Execute trunk-create or trunk-delete command.
     • Trunks can be used to aggregate network links at Layer 2 on the local switch. Use this command to create a new trunk.

PARAMETERS                   *pn_trunk-parameters*

          Parameter         Choices/Defaults                                                                         Comments
   pn_broadcast_level                        Specify a broadcast level in percent. The default value is 100%.
   -
   pn_clipassword                            Provide login password if user is not root.
   -
   pn_cliswitch             Default:         Target switch(es) to run the cli on.
   -                        "local"
   pn_cliusername                            Provide login username if user is not root.
   -
   pn_description                            Specify a description for the trunk configuration.
   -
   pn_edge_switch             Choices:
   boolean                    • no           Specify if the switch is an edge switch.
                              • yes
   pn_egress_rate_limit                      Specify an egress port data rate limit for the configuration.
   -
   pn_host                    Choices:
   boolean                    • no           Host facing port control setting.
                              • yes
   pn_jumbo                   Choices:
   boolean                    • no           Specify if the port can receive jumbo frames.
                              • yes
   pn_lacp_fallback           Choices:
   -                          • bundle       Specify the LACP fallback mode as bundles or individual.
                              • individual
   pn_lacp_fallback_timeout                  Specify the LACP fallback timeout in seconds. The range is between 30 and 60 seconds with a default value of 50 seconds.
   -
                              Choices:
   pn_lacp_mode               • off          Specify the LACP mode for the configuration.
   -                          • passive
                              • active
   pn_lacp_priority                          Specify the LACP priority. This is a number between 1 and 65535 with a default value of 32768.
   -
   pn_lacp_timeout            Choices:
   -                          • slow         Specify the LACP time out as slow (30 seconds) or fast (4seconds). The default value is slow.
                              • fast
   pn_loopback                Choices:
   boolean                    • no           Specify loopback if you want to use loopback.
                              • yes
   pn_loopvlans                              Specify a list of looping vlans.
   -
   pn_mirror_receive          Choices:
   boolean                    • no           Specify if the configuration receives mirrored traffic.
                              • yes
   pn_name                                   Specify the name for the trunk configuration.
   - / required
   pn_pause                   Choices:
   boolean                    • no           Specify if pause frames are sent.
                              • yes
   pn_port_macaddr                           Specify the MAC address of the port.
   -
   pn_ports                                  Specify the port number(s) for the link(s) to aggregate into the trunk.
   -                                         Required for trunk-create.
   pn_routing                 Choices:
   boolean                    • no           Specify if the port participates in routing on the network.
                              • yes
                              Choices:
                              • disable
                              • 10m
   pn_speed                   • 100m         Specify the port speed or disable the port.
   -                          • 1g
                              • 2.5g
                              • 10g
                              • 40g
   pn_unknown_mcast_level                    Specify an unknown multicast level in percent. The default value is 100%.
   -
   pn_unknown_ucast_level                    Specify an unknown unicast level in percent. The default value is 100%.
   -
                              Choices:
   state                      • present      State the action to perform. Use 'present' to create trunk, 'absent' to delete trunk and 'update' to modify trunk.
   - / required               • absent
                              • update

EXAMPLES                   *pn_trunk-examples*

 - name: create trunk
   pn_trunk:
     state: 'present'
     pn_name: 'spine-to-leaf'
     pn_ports: '11,12,13,14'

 - name: delete trunk
   pn_trunk:
     state: 'absent'
     pn_name: 'spine-to-leaf'

RETURN VALUES                   *pn_trunk-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key   Returned                                                                                    Description
   changed always   Indicates whether the CLI caused changes on the target.
   boolean
   command always   The CLI command run on the target node(s).
   string
   stderr  on error The set of error responses from the trunk command.
   list
   stdout  always   The set of responses from the trunk command.
   list

STATUS                   *pn_trunk-status*

     • This module will be removed in version 2.12. [deprecated]
     • For more information see DEPRECATED.

     • Pluribus Networks (@amitsi)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_trunk-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_trunk_module.html
