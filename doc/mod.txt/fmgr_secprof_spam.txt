SYNOPSIS                   *fmgr_secprof_spam*

     • Manage spam filter security profiles within FortiManager via API

PARAMETERS                   *fmgr_secprof_spam-parameters*

            Parameter            Choices/Defaults                                                                       Comments
   adom                         Default:           The ADOM the configuration should belong to.
   -                            "root"
   comment                                         Comment.
   -
   external                       Choices:
   -                              • disable        Enable/disable external Email inspection.
                                  • enable
   flow_based                     Choices:
   -                              • disable        Enable/disable flow-based spam filtering.
                                  • enable
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   gmail                                           Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   gmail_log                      Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   imap                                            Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   imap_action                    Choices:
   -                              • pass           Action for spam email.
                                  • tag
   imap_log                       Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
   imap_tag_msg                                    Subject text or header added to spam email.
   -
                                  Choices:
   imap_tag_type                  • subject        Tag subject or header for spam email.
   -                              • header         FLAG Based Options. Specify multiple in list form.
                                  • spaminfo
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   mapi                                            Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   mapi_action                    Choices:
   -                              • pass           Action for spam email.
                                  • discard
   mapi_log                       Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
                                  Choices:
   mode                           • add ←          Sets one of three modes for managing the object.
   -                              • set            Allows use of soft-adds instead of overwriting existing values
                                  • delete
                                  • update
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   msn_hotmail                                     Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   msn_hotmail_log                Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
   name                                            Profile name.
   -
                                  Choices:
                                  • bannedword
                                  • spamfsip
                                  • spamfssubmit
                                  • spamfschksum
   options                        • spamfsurl      None
   -                              • spamhelodns    FLAG Based Options. Specify multiple in list form.
                                  • spamraddrdns
                                  • spamrbl
                                  • spamhdrcheck
                                  • spamfsphish
                                  • spambwl
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   pop3                                            Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   pop3_action                    Choices:
   -                              • pass           Action for spam email.
                                  • tag
   pop3_log                       Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
   pop3_tag_msg                                    Subject text or header added to spam email.
   -
                                  Choices:
   pop3_tag_type                  • subject        Tag subject or header for spam email.
   -                              • header         FLAG Based Options. Specify multiple in list form.
                                  • spaminfo
   replacemsg_group                                Replacement message group.
   -
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   smtp                                            Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
                                  Choices:
   smtp_action                    • pass           Action for spam email.
   -                              • tag
                                  • discard
   smtp_hdrip                     Choices:
   -                              • disable        Enable/disable SMTP email header IP checks for spamfsip, spamrbl and spambwl filters.
                                  • enable
   smtp_local_override            Choices:
   -                              • disable        Enable/disable local filter to override SMTP remote check result.
                                  • enable
   smtp_log                       Choices:
   -                              • disable        Enable/disable logging.
                                  • enable
   smtp_tag_msg                                    Subject text or header added to spam email.
   -
                                  Choices:
   smtp_tag_type                  • subject        Tag subject or header for spam email.
   -                              • header         FLAG Based Options. Specify multiple in list form.
                                  • spaminfo
   spam_bwl_table                                  Anti-spam black/white list table ID.
   -
   spam_bword_table                                Anti-spam banned word table ID.
   -
   spam_bword_threshold                            Spam banned word threshold.
   -
   spam_filtering                 Choices:
   -                              • disable        Enable/disable spam filtering.
                                  • enable
   spam_iptrust_table                              Anti-spam IP trust table ID.
   -
   spam_log                       Choices:
   -                              • disable        Enable/disable spam logging for email filtering.
                                  • enable
   spam_log_fortiguard_response   Choices:
   -                              • disable        Enable/disable logging FortiGuard spam response.
                                  • enable
   spam_mheader_table                              Anti-spam MIME header table ID.
   -
   spam_rbl_table                                  Anti-spam DNSBL table ID.
   -
                                                   EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                   List of multiple child objects to be added. Expects a list of dictionaries.
   yahoo_mail                                      Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                               If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                   This object is MUTUALLY EXCLUSIVE with its options.
                                                   We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                   WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
   yahoo_mail_log                 Choices:
   -                              • disable        Enable/disable logging.
                                  • enable

NOTES                   *fmgr_secprof_spam-notes*

   Note

     • Full Documentation at https://ftnt-ansible-docs.readthedocs.io/en/latest/.

EXAMPLES                   *fmgr_secprof_spam-examples*

 - name: DELETE Profile
   fmgr_secprof_spam:
     name: "Ansible_Spam_Filter_Profile"
     mode: "delete"

 - name: Create FMGR_SPAMFILTER_PROFILE
   fmgr_secprof_spam:
     host: "{{ inventory_hostname }}"
     username: "{{ username }}"
     password: "{{ password }}"
     mode: "set"
     adom: "root"
     spam_log_fortiguard_response: "enable"
     spam_iptrust_table:
     spam_filtering: "enable"
     spam_bword_threshold: 10
     options: ["bannedword", "spamfsip", "spamfsurl", "spamrbl", "spamfsphish", "spambwl"]
     name: "Ansible_Spam_Filter_Profile"
     flow_based: "enable"
     external: "enable"
     comment: "Created by Ansible"
     gmail_log: "enable"
     spam_log: "enable"

RETURN VALUES                   *fmgr_secprof_spam-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   api_result always   full API response, includes status code and message
   string

STATUS                   *fmgr_secprof_spam-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Luke Weighall (@lweighall)
     • Andrew Welsh (@Ghilli3)
     • Jim Huber (@p4r4n0y1ng)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *fmgr_secprof_spam-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/fmgr_secprof_spam_module.html
