SYNOPSIS                   *cp_mgmt_host*

     • Manages host objects on Check Point devices including creating, updating and removing objects.
     • All operations are performed over Web Services API.

PARAMETERS                   *cp_mgmt_host-parameters*

         Parameter        Choices/Defaults                                                                          Comments
   auto_publish_session     Choices:
   boolean                  • no           Publish the current session if changes have been performed after task completes.
                            • yes
                            Choices:
                            • aquamarine
                            • black
                            • blue
                            • crete blue
                            • burlywood
                            • cyan
                            • dark green
                            • khaki
                            • orchid
                            • dark orange
                            • dark sea
                              green
                            • pink
                            • turquoise
                            • dark blue
                            • firebrick
                            • brown
   color                    • forest green
   string                   • gold         Color of the object. Should be one of existing colors.
                            • dark gold
                            • gray
                            • dark gray
                            • light green
                            • lemon
                              chiffon
                            • coral
                            • sea green
                            • sky blue
                            • magenta
                            • purple
                            • slate blue
                            • violet red
                            • navy blue
                            • olive
                            • orange
                            • red
                            • sienna
                            • yellow
   comments                                Comments string.
   string
                            Choices:
   details_level            • uid          The level of detail for some of the fields in the response can vary from showing only the UID value of the object to a fully detailed representation of
   string                   • standard     the object.
                            • full
   groups                                  Collection of group identifiers.
   list
   host_servers                            Servers Configuration.
   dictionary
    dns_server              Choices:
    boolean                 • no           Gets True if this server is a DNS Server.
                            • yes
    mail_server             Choices:
    boolean                 • no           Gets True if this server is a Mail Server.
                            • yes
    web_server              Choices:
    boolean                 • no           Gets True if this server is a Web Server.
                            • yes
    web_server_config                      Web Server configuration.
    dictionary
     additional_ports                      Server additional ports.
     list
     application_engines                   Application engines of this web server.
     list
     listen_standard_port   Choices:
     boolean                • no           Whether server listens to standard port.
                            • yes
                            Choices:
                            • sparc linux
     operating_system       • windows
     string                 • other        Operating System.
                            • x86 linux
                            • sparc
                              solaris
     protected_by                          Network object which protects this server identified by the name or UID.
     string
   ignore_errors            Choices:
   boolean                  • no           Apply changes ignoring errors. You won't be able to publish such a changes. If ignore-warnings flag was omitted - warnings will also be ignored.
                            • yes
   ignore_warnings          Choices:
   boolean                  • no           Apply changes ignoring warnings.
                            • yes
   interfaces                              Host interfaces.
   list
                            Choices:
                            • aquamarine
                            • black
                            • blue
                            • crete blue
                            • burlywood
                            • cyan
                            • dark green
                            • khaki
                            • orchid
                            • dark orange
                            • dark sea
                              green
                            • pink
                            • turquoise
                            • dark blue
                            • firebrick
                            • brown
    color                   • forest green
    string                  • gold         Color of the object. Should be one of existing colors.
                            • dark gold
                            • gray
                            • dark gray
                            • light green
                            • lemon
                              chiffon
                            • coral
                            • sea green
                            • sky blue
                            • magenta
                            • purple
                            • slate blue
                            • violet red
                            • navy blue
                            • olive
                            • orange
                            • red
                            • sienna
                            • yellow
    comments                               Comments string.
    string
                            Choices:
    details_level           • uid          The level of detail for some of the fields in the response can vary from showing only the UID value of the object to a fully detailed representation of
    string                  • standard     the object.
                            • full
    ignore_errors           Choices:
    boolean                 • no           Apply changes ignoring errors. You won't be able to publish such a changes. If ignore-warnings flag was omitted - warnings will also be ignored.
                            • yes
    ignore_warnings         Choices:
    boolean                 • no           Apply changes ignoring warnings.
                            • yes
    mask_length                            IPv4 or IPv6 network mask length. If both masks are required use mask-length4 and mask-length6 fields explicitly. Instead of IPv4 mask length it is
    integer                                possible to specify IPv4 mask itself in subnet-mask field.
    mask_length4                           IPv4 network mask length.
    integer
    mask_length6                           IPv6 network mask length.
    integer
    name                                   Interface name.
    string
    subnet                                 IPv4 or IPv6 network address. If both addresses are required use subnet4 and subnet6 fields explicitly.
    string
    subnet4                                IPv4 network address.
    string
    subnet6                                IPv6 network address.
    string
    subnet_mask                            IPv4 network mask.
    string
   ip_address                              IPv4 or IPv6 address. If both addresses are required use ipv4-address and ipv6-address fields explicitly.
   string
   ipv4_address                            IPv4 address.
   string
   ipv6_address                            IPv6 address.
   string
   name                                    Object name.
   string / required
   nat_settings                            NAT settings.
   dictionary
    auto_rule               Choices:
    boolean                 • no           Whether to add automatic address translation rules.
                            • yes
    hide_behind             Choices:
    string                  • gateway      Hide behind method. This parameter is not required in case "method" parameter is "static".
                            • ip-address
    install_on                             Which gateway should apply the NAT translation.
    string
    ip_address                             IPv4 or IPv6 address. If both addresses are required use ipv4-address and ipv6-address fields explicitly. This parameter is not required in case "method"
    string                                 parameter is "hide" and "hide-behind" parameter is "gateway".
    ipv4_address                           IPv4 address.
    string
    ipv6_address                           IPv6 address.
    string
    method                  Choices:
    string                  • hide         NAT translation method.
                            • static
   state                    Choices:
   string                   • present ←    State of the access rule (present or absent). Defaults to present.
                            • absent
   tags                                    Collection of tag identifiers.
   list
   version                                 Version of checkpoint. If not given one, the latest version taken.
   string
   wait_for_task            Choices:
   boolean                  • no           Wait for the task to end. Such as publish task.
                            • yes ←

EXAMPLES                   *cp_mgmt_host-examples*

 - name: add-host
   cp_mgmt_host:
     ip_address: 192.0.2.1
     name: New Host 1
     state: present

 - name: set-host
   cp_mgmt_host:
     color: green
     ipv4_address: 192.0.2.2
     name: New Host 1
     state: present

 - name: delete-host
   cp_mgmt_host:
     name: New Host 1
     state: absent

RETURN VALUES                   *cp_mgmt_host-return values*

   Common return values are documented here, the following are the fields unique to this module:

       Key                      Returned                                                                                 Description
   cp_mgmt_host always, except when deleting the object. The checkpoint object created or updated.
   dictionary

STATUS                   *cp_mgmt_host-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Or Soffer (@chkp-orso)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *cp_mgmt_host-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/cp_mgmt_host_module.html
