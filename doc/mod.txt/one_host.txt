SYNOPSIS                   *one_host*

     • Manages OpenNebula Hosts

REQUIREMENTS                   *one_host-requirements*

   The below requirements are needed on the host that executes this module.

     • pyone

PARAMETERS                   *one_host-parameters*

     Parameter    Choices/Defaults                                                                              Comments
   api_password                    The password or token for XMLRPC authentication.
   string                          If not specified then the value of the ONE_PASSWORD environment variable, if any, is used.
                                   aliases: api_token
   api_url                         The ENDPOINT URL of the XMLRPC server.
   string                          If not specified then the value of the ONE_URL environment variable, if any, is used.
                                   aliases: api_endpoint
   api_username                    The name of the user for XMLRPC authentication.
   string                          If not specified then the value of the ONE_USERNAME environment variable, if any, is used.
   cluster_id     Default:         The cluster ID.
   -              0
   cluster_name                    The cluster specified by name.
   -
   im_mad_name    Default:         The name of the information manager, this values are taken from the oned.conf with the tag name IM_MAD (name)
   -              "kvm"
   labels                          The labels for this host.
   -
   name                            Hostname of the machine to manage.
   - / required
                    Choices:       Takes the host to the desired lifecycle state.
                    • absent       If absent the host will be deleted from the cluster.
   state            • present ←    If present the host will be created in the cluster (includes enabled, disabled and offline states).
   -                • enabled      If enabled the host is fully operational.
                    • disabled     disabled, e.g. to perform maintenance operations.
                    • offline      offline, host is totally offline.
   template                        The template or attribute changes to merge into the host template.
   -                               aliases: attributes
   validate_certs   Choices:       Whether to validate the SSL certificates or not.
   boolean          • no           This parameter is ignored if PYTHONHTTPSVERIFY environment variable is used.
                    • yes ←
   vmm_mad_name   Default:         The name of the virtual machine manager mad name, this values are taken from the oned.conf with the tag name VM_MAD (name)
   -              "kvm"
   wait_timeout   Default:         Time to wait for the desired state to be reached before timeout, in seconds.
   integer        300

EXAMPLES                   *one_host-examples*

 - name: Create a new host in OpenNebula
   one_host:
     name: host1
     cluster_id: 1
     api_url: http://127.0.0.1:2633/RPC2

 - name: Create a host and adjust its template
   one_host:
     name: host2
     cluster_name: default
     template:
         LABELS:
             - gold
             - ssd
         RESERVED_CPU: -100

STATUS                   *one_host-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Rafael del Valle (@rvalle)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *one_host-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/one_host_module.html
