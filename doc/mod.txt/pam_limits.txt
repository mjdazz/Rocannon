SYNOPSIS                   *pam_limits*

     • The pam_limits module modifies PAM limits. The default file is /etc/security/limits.conf. For the full documentation, see man 5 limits.conf.

PARAMETERS                   *pam_limits-parameters*

   Parameter       Choices/Defaults                                                                                Comments
   backup       Choices:
   boolean      • no ←                    Create a backup file including the timestamp information so you can get the original file back if you somehow clobbered it incorrectly.
                • yes
   comment    Default:                    Comment associated with the limit.
   -          ""
   dest       Default:                    Modify the limits.conf path.
   -          "/etc/security/limits.conf"
   domain
   - /                                    A username, @groupname, wildcard, uid/gid range.
   required
                Choices:
                • core
                • data
                • fsize
                • memlock
                • nofile
                • rss
                • stack
   limit_item   • cpu
   - /          • nproc                   The limit to be set
   required     • as
                • maxlogins
                • maxsyslogins
                • priority
                • locks
                • sigpending
                • msgqueue
                • nice
                • rtprio
                • chroot
   limit_type   Choices:
   - /          • hard                    Limit type, see man 5 limits.conf for an explanation
   required     • soft
                • -
   use_max      Choices:                  If set to yes, the maximal value will be used or conserved. If the specified value is superior to the value in the file, file content is replaced with the
   boolean      • no ←                    new value, else content is not modified.
                • yes
   use_min      Choices:                  If set to yes, the minimal value will be used or conserved. If the specified value is inferior to the value in the file, file content is replaced with the
   boolean      • no ←                    new value, else content is not modified.
                • yes
   value
   - /                                    The value of the limit.
   required

NOTES                   *pam_limits-notes*

   Note

     • If dest file doesn’t exist, it is created.

EXAMPLES                   *pam_limits-examples*

 - name: Add or modify nofile soft limit for the user joe
   pam_limits:
     domain: joe
     limit_type: soft
     limit_item: nofile
     value: 64000

 - name: Add or modify fsize hard limit for the user smith. Keep or set the maximal value.
   pam_limits:
     domain: smith
     limit_type: hard
     limit_item: fsize
     value: 1000000
     use_max: yes

 - name: Add or modify memlock, both soft and hard, limit for the user james with a comment.
   pam_limits:
     domain: james
     limit_type: '-'
     limit_item: memlock
     value: unlimited
     comment: unlimited memory lock for james

 - name: Add or modify hard nofile limits for wildcard domain
   pam_limits:
     domain: '"'
     limit_type: hard
     limit_item: nofile
     value: 39693561

STATUS                   *pam_limits-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Sebastien Rohaut (@usawa)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pam_limits-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pam_limits_module.html
