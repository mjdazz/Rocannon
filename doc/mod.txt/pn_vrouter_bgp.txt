SYNOPSIS                   *pn_vrouter_bgp*

     • This module can be used to add Border Gateway Protocol neighbor to a vRouter modify Border Gateway Protocol neighbor to a vRouter and remove Border Gateway Protocol neighbor from a vRouter.

PARAMETERS                   *pn_vrouter_bgp-parameters*

             Parameter             Choices/Defaults                                                                      Comments
   pn_advertisement_interval                         Minimum interval between sending BGP routing updates.
   string
   pn_allowas_in                    Choices:
   boolean                          • no             Allow/reject routes with local AS in AS_PATH.
                                    • yes
   pn_bfd                           Choices:
   boolean                          • no ←           BFD protocol support for fault detection.
                                    • yes
   pn_bfd_multihop                  Choices:
   boolean                          • no             always use BFD multi-hop port for fault detection.
                                    • yes
   pn_cliswitch                                      Target switch to run the CLI on.
   string
   pn_connect_retry_interval                         BGP Connect retry interval (seconds).
   string
   pn_default_originate             Choices:
   boolean                          • no             announce default routes to the neighbor or not.
                                    • yes
   pn_ebgp_multihop                                  value for external BGP from 1 to 255.
   string
   pn_interface                                      Interface to reach the neighbor.
   string
   pn_max_prefix                                     maximum number of prefixes.
   string
   pn_max_prefix_warn_only          Choices:
   boolean                          • no             warn if the maximum number of prefixes is exceeded.
                                    • yes
   pn_multi_protocol                Choices:
   -                                • ipv4-unicast   Multi-protocol features.
                                    • ipv6-unicast
   pn_neighbor                                       IP address for BGP neighbor.
   string / required
   pn_neighbor_holdtime                              BGP Holdtime (seconds).
   string
   pn_neighbor_keepalive_interval                    BGP Keepalive interval (seconds).
   string
   pn_next_hop_self                 Choices:
   boolean                          • no             BGP next hop is self or not.
                                    • yes
   pn_no_route_map_in                                Remove ingress route-map from BGP neighbor.
   string
   pn_no_route_map_out                               Remove egress route-map from BGP neighbor.
   string
   pn_override_capability           Choices:
   boolean                          • no             override capability.
                                    • yes
   pn_password                                       password for MD5 BGP.
   string
   pn_prefix_list_in                                 prefixes used for filtering.
   string
   pn_prefix_list_out                                prefixes used for filtering outgoing packets.
   string
   pn_remote_as                                      BGP remote AS from 1 to 4294967295.
   string
   pn_route_map_in                                   route map in for nbr.
   string
   pn_route_map_out                                  route map out for nbr.
   string
   pn_route_reflector_client        Choices:
   boolean                          • no             set as route reflector client.
                                    • yes
   pn_send_community                Choices:
   boolean                          • no             send any community attribute to neighbor.
                                    • yes
   pn_soft_reconfig_inbound         Choices:
   boolean                          • no             soft reset to reconfigure inbound traffic.
                                    • yes
   pn_update_source                                  IP address of BGP packets required for peering over loopback interface.
   string
   pn_vrouter_name                                   name of service config.
   string / required
   pn_weight                                         default weight value between 0 and 65535 for the neighbor's routes.
   -
                                    Choices:
   state                            • present ←      vrouter-bgp configuration command.
   string                           • absent
                                    • update

EXAMPLES                   *pn_vrouter_bgp-examples*

 - name: "Add BGP to vRouter"
   pn_vrouter_bgp:
     state: 'present'
     pn_vrouter_name: 'sw01-vrouter'
     pn_neighbor: '105.104.104.1'
     pn_remote_as: 65000
     pn_bfd: true

 - name: "Remove BGP to vRouter"
   pn_vrouter_bgp:
     state: 'absent'
     pn_vrouter_name: 'sw01-vrouter'
     pn_neighbor: '105.104.104.1'

 - name: "Modify BGP to vRouter"
   pn_vrouter_bgp:
     state: 'update'
     pn_vrouter_name: 'sw01-vrouter'
     pn_neighbor: '105.104.104.1'
     pn_remote_as: 65000
     pn_bfd: false
     pn_allowas_in: true

RETURN VALUES                   *pn_vrouter_bgp-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key   Returned                                                                                    Description
   changed always   indicates whether the CLI caused changes on the target.
   boolean
   command always   the CLI command run on the target node.
   string
   stderr  on error set of error responses from the vrouter-bgp command.
   list
   stdout  always   set of responses from the vrouter-bgp command.
   list

STATUS                   *pn_vrouter_bgp-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Pluribus Networks (@rajaspachipulusu17)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_vrouter_bgp-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_vrouter_bgp_module.html
