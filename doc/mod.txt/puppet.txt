SYNOPSIS                   *puppet*

     • Runs puppet agent or apply in a reliable manner.

REQUIREMENTS                   *puppet-requirements*

   The below requirements are needed on the host that executes this module.

     • puppet

PARAMETERS                   *puppet-parameters*

      Parameter    Choices/Defaults                                                                             Comments
   certname                         The name to use when handling certificates.
   string
   debug             Choices:
   boolean           • no           Enable full debugging.
   added in 2.7      • yes
   environment                      Puppet environment to be used.
   string
   execute                          Execute a specific piece of Puppet code.
   string                           It has no effect with a puppetmaster.
   facter_basename Default:         Basename of the facter output file.
   string          "ansible"
   facts                            A dict of values to pass in as persistent external facter facts.
   dictionary
                     Choices:
   logdest           • all          Where the puppet logs should go, if puppet apply is being used.
   string            • stdout ←     all will go to both stdout and syslog.
                     • syslog
   manifest                         Path to the manifest file to run puppet apply on.
   string
   modulepath
   string                           Path to an alternate location for puppet modules.
   added in 2.4
   noop              Choices:       Override puppet.conf noop mode.
   boolean           • no           When yes, run Puppet agent with --noop switch set.
   added in 2.8      • yes          When no, run Puppet agent with --no-noop switch set.
                                    When unset (default), use default or puppet.conf value if defined.
   puppetmaster                     The hostname of the puppetmaster to contact.
   string
   summarize         Choices:
   boolean           • no           Whether to print a transaction summary.
   added in 2.7      • yes
   tags                             A list of puppet tags to be used.
   list
   timeout         Default:         How long to wait for puppet to finish.
   string          "30m"
   use_srv_records   Choices:
   boolean           • no           Toggles use_srv_records flag
   added in 2.9      • yes
   verbose           Choices:
   boolean           • no           Print extra information.
   added in 2.7      • yes

EXAMPLES                   *puppet-examples*

 - name: Run puppet agent and fail if anything goes wrong
   puppet:

 - name: Run puppet and timeout in 5 minutes
   puppet:
     timeout: 5m

 - name: Run puppet using a different environment
   puppet:
     environment: testing

 - name: Run puppet using a specific certname
   puppet:
     certname: agent01.example.com

 - name: Run puppet using a specific piece of Puppet code. Has no effect with a puppetmaster
   puppet:
     execute: include ::mymodule

 - name: Run puppet using a specific tags
   puppet:
     tags:
     - update
     - nginx

 - name: Run puppet agent in noop mode
   puppet:
     noop: yes

 - name: Run a manifest with debug, log to both syslog and stdout, specify module path
   puppet:
     modulepath: /etc/puppet/modules:/opt/stack/puppet-modules:/usr/share/openstack-puppet/modules
     logdest: all
     manifest: /var/lib/example/puppet_step_config.pp

STATUS                   *puppet-status*

     • This module is guaranteed to have backward compatible interface changes going forward. [stableinterface]
     • This module is maintained by the Ansible Community. [community]

     • Monty Taylor (@emonty)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *puppet-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/puppet_module.html
