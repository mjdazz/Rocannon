SYNOPSIS                   *utm_proxy_location*

     • Create, update or destroy a reverse_proxy location entry in SOPHOS UTM.
     • This module needs to have the REST Ability of the UTM to be activated.

PARAMETERS                   *utm_proxy_location-parameters*

         Parameter       Choices/Defaults                                                                          Comments
   access_control          Choices:
   string                  • 0 ←          whether to activate the access control for the location
                           • 1
   allowed_networks      Default:         A list of allowed networks
   list                  "REF_NetworkAny"
   auth_profile                           The reference name of the auth profile
   -
   backend               Default:         A list of backends that are connected with this location declaration
   -                     []
   be_path                                The path of the backend
   -
   comment                                The optional comment string
   -
   denied_networks       Default:         A list of denied network references
   -                     []
   headers                                A dictionary of additional headers to be sent to POST and PUT requests.
   dictionary                             Is needed for some modules
   hot_standby             Choices:
   boolean                 • no ←         Activate hot standby mode
                           • yes
   name                                   The name of the object. Will be used to identify the entry
   - / required
   path                  Default:         The path of the location
   -                     "/"
   state                   Choices:       The desired state of the object.
   string                  • absent       present will create or update an object
                           • present ←    absent will delete an object if it was present
   status                  Choices:
   boolean                 • no           Whether the location is active or not
                           • yes ←
   stickysession_id      Default:         The stickysession id
   -                     "ROUTEID"
   stickysession_status    Choices:
   boolean                 • no ←         Enable the stickysession
                           • yes
   utm_host                               The REST Endpoint of the Sophos UTM.
   string / required
   utm_port              Default:         The port of the REST interface.
   integer               4444
   utm_protocol            Choices:
   string                  • http         The protocol of the REST Endpoint.
                           • https ←
   utm_token                              The token used to identify at the REST-API. See https://www.sophos.com/en-us/medialibrary/PDFs/documentation/UTMonAWS/Sophos-UTM-RESTful-API.pdf?la=en,
   string / required                      Chapter 2.4.2.
   validate_certs          Choices:
   boolean                 • no           Whether the REST interface's ssl certificate should be verified or not.
                           • yes ←
   websocket_passthrough   Choices:
   boolean                 • no ←         Enable the websocket passthrough
                           • yes

EXAMPLES                   *utm_proxy_location-examples*

 - name: Create UTM proxy_location
   utm_proxy_backend:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestLocationEntry
     backend: REF_OBJECT_STRING
     state: present

 - name: Remove UTM proxy_location
   utm_proxy_backend:
     utm_host: sophos.host.name
     utm_token: abcdefghijklmno1234
     name: TestLocationEntry
     state: absent

RETURN VALUES                   *utm_proxy_location-return values*

   Common return values are documented here, the following are the fields unique to this module:

             Key           Returned                                                                            Description
   result                  success  The utm object that was created
   complex
     _locked                        Whether or not the object is currently locked
     boolean
     _ref                           The reference name of the object
     string
     _type                          The type of the object
     string
     access_control                 Whether to use access control state
     string
     allowed_networks               List of allowed network reference names
     list
     auth_profile                   The auth profile reference name
     string
     backend                        The backend reference name
     string
     be_path                        The backend path
     string
     comment                        The comment string
     string
     denied_networks                The list of the denied network names
     list
     hot_standby                    Use hot standy
     boolean
     name                           The name of the object
     string
     path                           Path name
     string
     status                         Whether the object is active or not
     boolean
     stickysession_id               The identifier of the stickysession
     string
     stickysession_status           Whether to use stickysession or not
     boolean
     websocket_passthrough          Whether websocket passthrough will be used or not
     boolean

STATUS                   *utm_proxy_location-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Johannes Brunswicker (@MatrixCrawler)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *utm_proxy_location-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/utm_proxy_location_module.html
