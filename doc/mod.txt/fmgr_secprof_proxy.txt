SYNOPSIS                   *fmgr_secprof_proxy*

     • Manage proxy security profiles for FortiGates via FortiManager using the FMG API with playbooks

PARAMETERS                   *fmgr_secprof_proxy-parameters*

             Parameter                Choices/Defaults                                                                      Comments
   adom                          Default:                   The ADOM the configuration should belong to.
   -                             "root"
                                   Choices:                 Actions to take on the HTTP client-IP header in forwarded requests| forwards (pass), adds, or removes the HTTP
   header_client_ip                • pass                   header.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP front-end-HTTPS header in forwarded requests| forwards (pass), adds, or removes the
   header_front_end_https          • pass                   HTTP header.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP via header in forwarded requests| forwards (pass), adds, or removes the HTTP header
   header_via_request              • pass                   .
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP via header in forwarded responses| forwards (pass), adds, or removes the HTTP heade
   header_via_response             • pass                   r.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP x-authenticated-groups header in forwarded requests| forwards (pass), adds, or remo
   header_x_authenticated_groups   • pass                   ves the HTTP header.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP x-authenticated-user header in forwarded requests| forwards (pass), adds, or remove
   header_x_authenticated_user     • pass                   s the HTTP header.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                   Choices:                 Action to take on the HTTP x-forwarded-for header in forwarded requests| forwards (pass), adds, or removes the
   header_x_forwarded_for          • pass                   HTTP header.
   -                               • add                    choice | pass | Forward the same HTTP header.
                                   • remove                 choice | add | Add the HTTP header.
                                                            choice | remove | Remove the HTTP header.
                                                            EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                            List of multiple child objects to be added. Expects a list of dictionaries.
   headers                                                  Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                                        If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                            This object is MUTUALLY EXCLUSIVE with its options.
                                                            We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                            WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
                                   Choices:                 Action when HTTP the header forwarded.
   headers_action                  • add-to-request         choice | add-to-request | Add the HTTP header to request.
   -                               • add-to-response        choice | add-to-response | Add the HTTP header to response.
                                   • remove-from-request    choice | remove-from-request | Remove the HTTP header from request.
                                   • remove-from-response   choice | remove-from-response | Remove the HTTP header from response.
   headers_content                                          HTTP header's content.
   -
   headers_name                                             HTTP forwarded header name.
   -
   log_header_change               Choices:                 Enable/disable logging HTTP header changes.
   -                               • disable                choice | disable | Disable Enable/disable logging HTTP header changes.
                                   • enable                 choice | enable | Enable Enable/disable logging HTTP header changes.
                                   Choices:
   mode                            • add ←                  Sets one of three modes for managing the object.
   -                               • set                    Allows use of soft-adds instead of overwriting existing values
                                   • delete
                                   • update
   name                                                     Profile name.
   -
   strip_encoding                  Choices:                 Enable/disable stripping unsupported encoding from the request header.
   -                               • disable                choice | disable | Disable stripping of unsupported encoding from the request header.
                                   • enable                 choice | enable | Enable stripping of unsupported encoding from the request header.

NOTES                   *fmgr_secprof_proxy-notes*

   Note

     • Full Documentation at https://ftnt-ansible-docs.readthedocs.io/en/latest/.

EXAMPLES                   *fmgr_secprof_proxy-examples*

 - name: DELETE Profile
   fmgr_secprof_proxy:
     name: "Ansible_Web_Proxy_Profile"
     mode: "delete"

 - name: CREATE Profile
   fmgr_secprof_proxy:
     name: "Ansible_Web_Proxy_Profile"
     mode: "set"
     header_client_ip: "pass"
     header_front_end_https: "add"
     header_via_request: "remove"
     header_via_response: "pass"
     header_x_authenticated_groups: "add"
     header_x_authenticated_user: "remove"
     strip_encoding: "enable"
     log_header_change: "enable"
     header_x_forwarded_for: "pass"
     headers_action: "add-to-request"
     headers_content: "test"
     headers_name: "test_header"

RETURN VALUES                   *fmgr_secprof_proxy-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   api_result always   full API response, includes status code and message
   string

STATUS                   *fmgr_secprof_proxy-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Luke Weighall (@lweighall)
     • Andrew Welsh (@Ghilli3)
     • Jim Huber (@p4r4n0y1ng)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *fmgr_secprof_proxy-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/fmgr_secprof_proxy_module.html
