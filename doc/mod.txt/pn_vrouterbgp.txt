DEPRECATED                   *pn_vrouterbgp*

                              Removed in Ansible:
                  version: 2.12
        Why:      Doesn’t support latest Pluribus Networks netvisor
    Alternative:  Latest modules will be pushed in Ansible future versions.

SYNOPSIS                   *pn_vrouterbgp-synopsis*

     • Execute vrouter-bgp-add, vrouter-bgp-remove, vrouter-bgp-modify command.
     • Each fabric, cluster, standalone switch, or virtual network (VNET) can provide its tenants with a vRouter service that forwards traffic between networks and implements Layer 4 protocols.

PARAMETERS                   *pn_vrouterbgp-parameters*

         Parameter        Choices/Defaults                                                                          Comments
   pn_bfd                   Choices:
   boolean                  • no           Specify if you want BFD protocol support for fault detection.
                            • yes
   pn_clipassword                          Provide login password if user is not root.
   -
   pn_cliswitch           Default:         Target switch(es) to run the cli on.
   -                      "local"
   pn_cliusername                          Provide login username if user is not root.
   -
   pn_default_originate     Choices:
   boolean                  • no           Specify if you want announce default routes to the neighbor or not.
                            • yes
   pn_ebgp                                 Specify a value for external BGP to accept or attempt BGP connections to external peers, not directly connected, on the network. This is a value between 1
   -                                       and 255.
   pn_holdtime                             Specify BGP neighbor holdtime in seconds.
   -
   pn_keepalive                            Specify BGP neighbor keepalive interval in seconds.
   -
   pn_max_prefix                           Specify the maximum number of prefixes.
   -
   pn_max_prefix_warn       Choices:
   boolean                  • no           Specify if you want a warning message when the maximum number of prefixes is exceeded.
                            • yes
   pn_multiprotocol         Choices:
   -                        • ipv4-unicast Specify a multi-protocol for BGP.
                            • ipv6-unicast
   pn_neighbor                             Specify a neighbor IP address to use for BGP.
   -                                       Required for vrouter-bgp-add.
   pn_next_hop_self         Choices:
   boolean                  • no           Specify if the next-hop is the same router or not.
                            • yes
   pn_override_capability   Choices:
   boolean                  • no           Specify if you want to override capability.
                            • yes
   pn_password                             Specify a password, if desired.
   -
   pn_prefix_listin                        Specify the prefix list to filter traffic inbound.
   -
   pn_prefix_listout                       Specify the prefix list to filter traffic outbound.
   -
   pn_remote_as                            Specify the remote Autonomous System(AS) number. This value is between 1 and 4294967295.
   -                                       Required for vrouter-bgp-add.
   pn_route_mapin                          Specify inbound route map for neighbor.
   -
   pn_route_mapout                         Specify outbound route map for neighbor.
   -
   pn_route_reflector       Choices:
   boolean                  • no           Specify if a route reflector client is used.
                            • yes
   pn_soft_reconfig         Choices:
   boolean                  • no           Specify if you want a soft reconfiguration of inbound traffic.
                            • yes
   pn_vrouter_name                         Specify a name for the vRouter service.
   - / required
   pn_weight                               Specify a default weight value between 0 and 65535 for the neighbor routes.
   -
                            Choices:
   state                    • present      State the action to perform. Use 'present' to add bgp, 'absent' to remove bgp and 'update' to modify bgp.
   - / required             • absent
                            • update

EXAMPLES                   *pn_vrouterbgp-examples*

 - name: add vrouter-bgp
   pn_vrouterbgp:
     state: 'present'
     pn_vrouter_name: 'ansible-vrouter'
     pn_neighbor: 104.104.104.1
     pn_remote_as: 1800

 - name: remove vrouter-bgp
   pn_vrouterbgp:
     state: 'absent'
     pn_name: 'ansible-vrouter'

RETURN VALUES                   *pn_vrouterbgp-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key   Returned                                                                                    Description
   changed always   Indicates whether the CLI caused changes on the target.
   boolean
   command always   The CLI command run on the target node(s).
   string
   stderr  on error The set of error responses from the vrouterbgp command.
   list
   stdout  always   The set of responses from the vrouterbpg command.
   list

STATUS                   *pn_vrouterbgp-status*

     • This module will be removed in version 2.12. [deprecated]
     • For more information see DEPRECATED.

     • Pluribus Networks (@amitsi)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_vrouterbgp-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_vrouterbgp_module.html
