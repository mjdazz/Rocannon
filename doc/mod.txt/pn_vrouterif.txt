DEPRECATED                   *pn_vrouterif*

                              Removed in Ansible:
                  version: 2.12
        Why:      Doesn’t support latest Pluribus Networks netvisor
    Alternative:  Latest modules will be pushed in Ansible future versions.

SYNOPSIS                   *pn_vrouterif-synopsis*

     • Execute vrouter-interface-add, vrouter-interface-remove, vrouter-interface-modify command.
     • You configure interfaces to vRouter services on a fabric, cluster, standalone switch or virtual network(VNET).

PARAMETERS                   *pn_vrouterif-parameters*

       Parameter     Choices/Defaults                                                                            Comments
   pn_alias                           Specify an alias for the interface.
   -
                       Choices:
   pn_assignment       • none
   -                   • dhcp         Specify the DHCP method for IP address assignment.
                       • dhcpv6
                       • autov6
   pn_clipassword                     Provide login password if user is not root.
   -
   pn_cliswitch      Default:         Target switch to run the cli on.
   -                 "local"
   pn_cliusername                     Provide login username if user is not root.
   -
   pn_exclusive        Choices:       Specify if the interface is exclusive to the configuration. Exclusive means that other configurations cannot use the interface. Exclusive is specified when you
   boolean             • no           configure the interface as span interface and allows higher throughput through the interface.
                       • yes
                       Choices:
   pn_interface        • mgmt         Specify if the interface is management, data or span interface.
   -                   • data
                       • span
   pn_interface_ip                    Specify the IP address of the interface in x.x.x.x/n format.
   -
   pn_l3port                          Specify a Layer 3 port for the interface.
   -
   pn_nic_enable       Choices:
   boolean             • no           Specify if the NIC is enabled or not
                       • yes
   pn_nic_str                         Specify the type of NIC. Used for vrouter-interface remove/modify.
   -
   pn_secondary_macs                  Specify a secondary MAC address for the interface.
   -
   pn_vlan                            Specify the VLAN identifier. This is a value between 1 and 4092.
   -
   pn_vrouter_name                    Specify the name of the vRouter interface.
   - / required
   pn_vrrp_adv_int                    Specify a VRRP advertisement interval in milliseconds. The range is from 30 to 40950 with a default value of 1000.
   -
   pn_vrrp_id                         Specify the ID for the VRRP interface. The IDs on both vRouters must be the same IS number.
   -
   pn_vrrp_priority                   Specify the priority for the VRRP interface. This is a value between 1 (lowest) and 255 (highest).
   -
   pn_vxlan                           Specify the VXLAN identifier. This is a value between 1 and 16777215.
   -
                       Choices:
   state               • present      State the action to perform. Use 'present' to add vrouter interface, 'absent' to remove vrouter interface and 'update' to modify vrouter interface.
   - / required        • absent
                       • update

EXAMPLES                   *pn_vrouterif-examples*

 - name: Add vrouter-interface
   pn_vrouterif:
     pn_cliusername: admin
     pn_clipassword: admin
     state: 'present'
     pn_vrouter_name: 'ansible-vrouter'
     pn_interface_ip: 101.101.101.2/24
     pn_vlan: 101

 - name: Add VRRP..
   pn_vrouterif:
     pn_cliusername: admin
     pn_clipassword: admin
     state: 'present'
     pn_vrouter_name: 'ansible-vrouter'
     pn_interface_ip: 101.101.101.2/24
     pn_vrrp_ip: 101.101.101.1/24
     pn_vrrp_priority: 100
     pn_vlan: 101

 - name: Remove vrouter-interface
   pn_vrouterif:
     pn_cliusername: admin
     pn_clipassword: admin
     state: 'absent'
     pn_vrouter_name: 'ansible-vrouter'
     pn_interface_ip: 101.101.101.2/24

RETURN VALUES                   *pn_vrouterif-return values*

   Common return values are documented here, the following are the fields unique to this module:

     Key    Returned                                                                                    Description
   changed always     Indicates whether the CLI caused changes on the target.
   boolean
   command always     The CLI command run on the target node(s).
   string
   stderr  on error   The set of error responses from the vrouterif command.
   string
   stdout  on success The set of responses from the vrouterif command.
   list

STATUS                   *pn_vrouterif-status*

     • This module will be removed in version 2.12. [deprecated]
     • For more information see DEPRECATED.

     • Pluribus Networks (@amitsi)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *pn_vrouterif-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/pn_vrouterif_module.html
