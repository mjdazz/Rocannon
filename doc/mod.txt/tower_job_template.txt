SYNOPSIS                   *tower_job_template*

     • Create, update, or destroy Ansible Tower job templates. See https://www.ansible.com/tower for an overview.

REQUIREMENTS                   *tower_job_template-requirements*

   The below requirements are needed on the host that executes this module.

     • ansible-tower-cli >= 3.0.2

PARAMETERS                   *tower_job_template-parameters*

          Parameter        Choices/Defaults                                                                         Comments
   ask_credential            Choices:
   boolean                   • no ←         Prompt user for credential on launch.
                             • yes
   ask_diff_mode             Choices:
   boolean                   • no ←         Prompt user to enable diff mode (show changes) to files when supported by modules.
   added in 2.7              • yes
   ask_extra_vars            Choices:
   boolean                   • no ←         Prompt user for (extra_vars) on launch.
                             • yes
   ask_inventory             Choices:
   boolean                   • no ←         Prompt user for inventory on launch.
                             • yes
   ask_job_type              Choices:
   boolean                   • no ←         Prompt user for job type on launch.
                             • yes
   ask_limit                 Choices:
   boolean                   • no ←         Prompt user for a limit on launch.
   added in 2.7              • yes
   ask_skip_tags             Choices:
   boolean                   • no ←         Prompt user for job tags to skip on launch.
   added in 2.7              • yes
   ask_tags                  Choices:
   boolean                   • no ←         Prompt user for job tags on launch.
                             • yes
   ask_verbosity             Choices:
   boolean                   • no ←         Prompt user to choose a verbosity level on launch.
   added in 2.7              • yes
   become_enabled            Choices:
   boolean                   • no ←         Activate privilege escalation.
                             • yes
   concurrent_jobs_enabled   Choices:
   boolean                   • no ←         Allow simultaneous runs of the job template.
   added in 2.7              • yes
   credential
   -                                        Name of the credential to use for the job template.
   added in 2.7
   description                              Description to use for the job template.
   -
   diff_mode_enabled         Choices:
   boolean                   • no ←         Enable diff mode for the job template.
   added in 2.7              • yes
   extra_vars_path                          Path to the extra_vars YAML file.
   -
   fact_caching_enabled      Choices:
   boolean                   • no ←         Enable use of fact caching for the job template.
   added in 2.7              • yes
   force_handlers_enabled    Choices:
   boolean                   • no ←         Enable forcing playbook handlers to run even if a task fails.
   added in 2.7              • yes
   forks                                    The number of parallel or simultaneous processes to use while executing the playbook.
   -
   host_config_key                          Allow provisioning callbacks using this host config key.
   -
   inventory                                Name of the inventory to use for the job template.
   -
   job_tags                                 Comma separated list of the tags to use for the job template.
   -
                             Choices:
   job_type                  • run          The job type to use for the job template.
   - / required              • check
                             • scan
   limit                                    A host pattern to further constrain the list of hosts managed or affected by the playbook
   -
   name                                     Name to use for the job template.
   - / required
   playbook                                 Path to the playbook to use for the job template within the project provided.
   - / required
   project                                  Name of the project to use for the job template.
   - / required
   skip_tags                                Comma separated list of the tags to skip for the job template.
   -
   start_at_task
   -                                        Start the playbook at the task matching this name.
   added in 2.7
   state                     Choices:
   -                         • present ←    Desired state of the resource.
                             • absent
   survey_enabled            Choices:
   boolean                   • no ←         Enable a survey on the job template.
   added in 2.7              • yes
   survey_spec
   dictionary                               JSON/YAML dict formatted survey definition.
   added in 2.8
   tower_config_file                        Path to the Tower config file.
   path
   tower_host                               URL to your Tower instance.
   string
   tower_password                           Password for your Tower instance.
   string
   tower_username                           Username for your Tower instance.
   string
                             Choices:       Whether to allow insecure connections to Tower.
   validate_certs            • no           If no, SSL certificates will not be validated.
   boolean                   • yes          This should only be used on personally controlled sites using self-signed certificates.
                                            aliases: tower_verify_ssl
   vault_credential
   -                                        Name of the vault credential to use for the job template.
   added in 2.7
                             Choices:
                             • 0 ←
   verbosity                 • 1            Control the output level Ansible produces as the playbook runs. 0 - Normal, 1 - Verbose, 2 - More Verbose, 3 - Debug, 4 - Connection Debug.
   -                         • 2
                             • 3
                             • 4

NOTES                   *tower_job_template-notes*

   Note

     • JSON for survey_spec can be found in Tower API Documentation. See
       https://docs.ansible.com/ansible-tower/latest/html/towerapi/api_ref.html#/Job_Templates/Job_Templates_job_templates_survey_spec_create for POST operation payload example.
     • If no config_file is provided we will attempt to use the tower-cli library defaults to find your Tower host information.
     • config_file should contain Tower configuration in the following format host=hostname username=username password=password

EXAMPLES                   *tower_job_template-examples*

 - name: Create tower Ping job template
   tower_job_template:
     name: "Ping"
     job_type: "run"
     inventory: "Local"
     project: "Demo"
     playbook: "ping.yml"
     credential: "Local"
     state: "present"
     tower_config_file: "~/tower_cli.cfg"
     survey_enabled: yes
     survey_spec: "{{ lookup('file', 'my_survey.json') }}"

STATUS                   *tower_job_template-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Wayne Witzel III (@wwitzel3)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *tower_job_template-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/tower_job_template_module.html
