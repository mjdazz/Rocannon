SYNOPSIS                   *idrac_firmware*

     • Update the Firmware by connecting to a network share (either CIFS or NFS) that contains a catalog of available updates.
     • Network share should contain a valid repository of Update Packages (DUPs) and a catalog file describing the DUPs.
     • All applicable updates contained in the repository are applied to the system.
     • This feature is available only with iDRAC Enterprise License.

REQUIREMENTS                   *idrac_firmware-requirements*

   The below requirements are needed on the host that executes this module.

     • omsdk
     • python >= 2.7.5

PARAMETERS                   *idrac_firmware-parameters*

       Parameter     Choices/Defaults                                                                            Comments
   catalog_file_name Default:         Catalog file name relative to the share_name.
   string            "Catalog.xml"
   idrac_ip                           iDRAC IP Address.
   string / required
   idrac_password                     iDRAC user password.
   string / required                  aliases: idrac_pwd
   idrac_port        Default:         iDRAC port.
   integer           443
   idrac_user                         iDRAC username.
   string / required
   job_wait            Choices:
   boolean             • no           Whether to wait for job completion or not.
                       • yes ←
   reboot              Choices:
   boolean             • no ←         Whether to reboots after applying the updates or not.
                       • yes
   share_mnt                          Local mount path of the network share with read-write permission for ansible user. This option is mandatory for Network Share.
   string / required
   share_name                         CIFS or NFS Network share.
   string / required
   share_password                     Network share user password. This option is mandatory for CIFS Network Share.
   string                             aliases: share_pwd
   share_user                         Network share user in the format '[email protected]' or 'domain\\user' if user is part of a domain else 'user'. This option is mandatory for CIFS Network
   string                             Share.

EXAMPLES                   *idrac_firmware-examples*

 ---
 - name: Update firmware from repository on a Network Share
   idrac_firmware:
        idrac_ip: "192.168.0.1"
        idrac_user: "user_name"
        idrac_password: "user_password"
        share_name: "192.168.0.0:/share"
        share_user: "share_user_name"
        share_password: "share_user_pwd"
        share_mnt: "/mnt/share"
        reboot: True
        job_wait: True
        catalog_file_name: "Catalog.xml"

RETURN VALUES                   *idrac_firmware-return values*

   Common return values are documented here, the following are the fields unique to this module:

        Key      Returned                                                                                 Description
   msg                    Over all firmware update status.
   string        always   Sample:
                          Successfully updated the firmware.
                          Firmware Update job and progress details from the iDRAC.
   update_status success  Sample:
   dictionary             {'InstanceID': 'JID_XXXXXXXXXXXX', 'JobState': 'Completed', 'Message': 'Job completed successfully.', 'MessageId': 'REDXXX', 'Name': 'Repository Update', 'JobStartTime':
                          'NA', 'Status': 'Success'}

STATUS                   *idrac_firmware-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Rajeev Arakkal (@rajeevarakkal)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *idrac_firmware-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/idrac_firmware_module.html
