SYNOPSIS                   *infini_fs*

     • This module creates, deletes or modifies filesystems on Infinibox.

REQUIREMENTS                   *infini_fs-requirements*

   The below requirements are needed on the host that executes this module.

     • capacity
     • infinisdk
     • python >= 2.7

PARAMETERS                   *infini_fs-parameters*

       Parameter     Choices/Defaults                                                                            Comments
   name                               File system name.
   - / required
   password                           Infinibox User password.
   string
   pool                               Pool that will host file system.
   - / required
   size                               File system size in MB, GB or TB units. See examples.
   -
   state               Choices:
   -                   • present ←    Creates/Modifies file system when present or removes when absent.
                       • absent
   system                             Infinibox Hostname or IPv4 Address.
   string / required
   user                               Infinibox User username with sufficient priveledges ( see notes ).
   -

NOTES                   *infini_fs-notes*

   Note

     • This module requires infinisdk python library
     • You must set INFINIBOX_USER and INFINIBOX_PASSWORD environment variables if user and password arguments are not passed to the module directly
     • Ansible uses the infinisdk configuration file ~/.infinidat/infinisdk.ini if no credentials are provided. See http://infinisdk.readthedocs.io/en/latest/getting_started.html

EXAMPLES                   *infini_fs-examples*

 - name: Create new file system named foo under pool named bar
   infini_fs:
     name: foo
     size: 1TB
     pool: bar
     state: present
     user: admin
     password: secret
     system: ibox001

STATUS                   *infini_fs-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Gregory Shulov (@GR360RY)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *infini_fs-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/infini_fs_module.html
