SYNOPSIS                   *ipmi_boot*

     • Use this module to manage order of boot devices

REQUIREMENTS                   *ipmi_boot-requirements*

   The below requirements are needed on the host that executes this module.

     • python >= 2.6
     • pyghmi

PARAMETERS                   *ipmi_boot-parameters*

   Parameter  Choices/Defaults                                                                                Comments
                Choices:
                • network --
                  Request
                  network boot
                • floppy --
                  Boot from
                  floppy
                • hd -- Boot
                  from hard
                  drive
                • safe -- Boot
                  from hard
                  drive,
   bootdev        requesting
   - /            'safe mode'  Set boot device to use on next reboot
   required     • optical --
                  boot from
                  CD/DVD/BD
                  drive
                • setup --
                  Boot into
                  setup
                  utility
                • default --
                  remove any
                  IPMI
                  directed
                  boot device
                  request
   name
   - /                         Hostname or ip address of the BMC.
   required
   password
   - /                         Password to connect to the BMC.
   required
   persistent   Choices:
   boolean      • no ←         If set, ask that system firmware uses this device beyond next boot. Be aware many systems do not honor this.
                • yes
   port       Default:         Remote RMCP port.
   -          623
                Choices:
                • present --
                  Request
                  system turn
                  on
   state        • absent --    Whether to ensure that boot devices is desired.
   -              Request
                  system turn
                  on

              Default:
              "present"
   uefiboot     Choices:       If set, request UEFI boot explicitly. Strictly speaking, the spec suggests that if not set, the system should BIOS boot and offers no "don't care" option. In
   boolean      • no ←         practice, this flag not being set does not preclude UEFI boot on any system I've encountered.
                • yes
   user
   - /                         Username to use to connect to the BMC.
   required

EXAMPLES                   *ipmi_boot-examples*

 # Ensure bootdevice is HD.
 - ipmi_boot:
     name: test.testdomain.com
     user: admin
     password: password
     bootdev: hd

 # Ensure bootdevice is not Network
 - ipmi_boot:
     name: test.testdomain.com
     user: admin
     password: password
     bootdev: network
     state: absent

RETURN VALUES                   *ipmi_boot-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   bootdev             The boot device name which will be used beyond next boot.
   string     success  Sample:
                       default
   persistent success  If True, system firmware will use this device beyond next boot.
   boolean
   uefimode   success  If True, system firmware will use UEFI boot explicitly beyond next boot.
   boolean

STATUS                   *ipmi_boot-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Bulat Gaifullin (@bgaifullin) <gaifullinbf@gmail.com>

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *ipmi_boot-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/ipmi_boot_module.html
