SYNOPSIS                   *fmgr_secprof_ips*

     • Managing IPS security profiles in FortiManager

PARAMETERS                   *fmgr_secprof_ips-parameters*

           Parameter           Choices/Defaults                                                                        Comments
   adom                       Default:            The ADOM the configuration should belong to.
   -                          "root"
   block_malicious_url          Choices:
   -                            • disable         Enable/disable malicious URL blocking.
                                • enable
   comment                                        Comment.
   -
                                                  EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                  List of multiple child objects to be added. Expects a list of dictionaries.
   entries                                        Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                              If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                  This object is MUTUALLY EXCLUSIVE with its options.
                                                  We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                  WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
                                Choices:
   entries_action               • pass
   -                            • block           Action taken with traffic in which signatures are detected.
                                • reset
                                • default
   entries_application                            Applications to be protected. set application ? lists available applications. all includes all applications. other includes all unlisted
   -                                              applications.
   entries_exempt_ip_dst_ip                       Destination IP address and netmask.
   -
   entries_exempt_ip_src_ip                       Source IP address and netmask.
   -
   entries_location                               Protect client or server traffic.
   -
   entries_log                  Choices:
   -                            • disable         Enable/disable logging of signatures included in filter.
                                • enable
   entries_log_attack_context   Choices:
   -                            • disable         Enable/disable logging of attack context| URL buffer, header buffer, body buffer, packet buffer.
                                • enable
   entries_log_packet           Choices:
   -                            • disable         Enable/disable packet logging. Enable to save the packet that triggers the filter. You can download the packets in pcap format for diagnostic use.
                                • enable
   entries_os                                     Operating systems to be protected. all includes all operating systems. other includes all unlisted operating systems.
   -
   entries_protocol                               Protocols to be examined. set protocol ? lists available protocols. all includes all protocols. other includes all unlisted protocols.
   -
   entries_quarantine           Choices:
   -                            • none            Quarantine method.
                                • attacker
   entries_quarantine_expiry                      Duration of quarantine.
   -
   entries_quarantine_log       Choices:
   -                            • disable         Enable/disable quarantine logging.
                                • enable
   entries_rate_count                             Count of the rate.
   -
   entries_rate_duration                          Duration (sec) of the rate.
   -
   entries_rate_mode            Choices:
   -                            • periodical      Rate limit mode.
                                • continuous
                                Choices:
                                • none
   entries_rate_track           • src-ip          Track the packet protocol field.
   -                            • dest-ip
                                • dhcp-client-mac
                                • dns-domain
   entries_rule                                   Identifies the predefined or custom IPS signatures to add to the sensor.
   -
   entries_severity                               Relative severity of the signature, from info to critical. Log messages generated by the signature include the severity.
   -
                                Choices:
   entries_status               • disable         Status of the signatures included in filter. default enables the filter and only use filters with default status of enable. Filters with default
   -                            • enable          status of disable will not be used.
                                • default
   extended_log                 Choices:
   -                            • disable         Enable/disable extended logging.
                                • enable
                                                  EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                  List of multiple child objects to be added. Expects a list of dictionaries.
   filter                                         Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                              If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                  This object is MUTUALLY EXCLUSIVE with its options.
                                                  We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                  WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
                                Choices:
   filter_action                • pass
   -                            • block           Action of selected rules.
                                • default
                                • reset
   filter_application                             Vulnerable application filter.
   -
   filter_location                                Vulnerability location filter.
   -
   filter_log                   Choices:
   -                            • disable         Enable/disable logging of selected rules.
                                • enable
   filter_log_packet            Choices:
   -                            • disable         Enable/disable packet logging of selected rules.
                                • enable
   filter_name                                    Filter name.
   -
   filter_os                                      Vulnerable OS filter.
   -
   filter_protocol                                Vulnerable protocol filter.
   -
   filter_quarantine            Choices:
   -                            • none            Quarantine IP or interface.
                                • attacker
   filter_quarantine_expiry                       Duration of quarantine in minute.
   -
   filter_quarantine_log        Choices:
   -                            • disable         Enable/disable logging of selected quarantine.
                                • enable
   filter_severity                                Vulnerability severity filter.
   -
                                Choices:
   filter_status                • disable         Selected rules status.
   -                            • enable
                                • default
                                Choices:
   mode                         • add ←           Sets one of three modes for managing the object.
   -                            • set             Allows use of soft-adds instead of overwriting existing values
                                • delete
                                • update
   name                                           Sensor name.
   -
                                                  EXPERTS ONLY! KNOWLEDGE OF FMGR JSON API IS REQUIRED!
                                                  List of multiple child objects to be added. Expects a list of dictionaries.
   override                                       Dictionaries must use FortiManager API parameters, not the ansible ones listed below.
   -                                              If submitted, all other prefixed sub-parameters ARE IGNORED.
                                                  This object is MUTUALLY EXCLUSIVE with its options.
                                                  We expect that you know what you are doing with these list parameters, and are leveraging the JSON API Guide.
                                                  WHEN IN DOUBT, USE THE SUB OPTIONS BELOW INSTEAD TO CREATE OBJECTS WITH MULTIPLE TASKS
                                Choices:
   override_action              • pass            Action of override rule.
   -                            • block
                                • reset
   override_exempt_ip_dst_ip                      Destination IP address and netmask.
   -
   override_exempt_ip_src_ip                      Source IP address and netmask.
   -
   override_log                 Choices:
   -                            • disable         Enable/disable logging.
                                • enable
   override_log_packet          Choices:
   -                            • disable         Enable/disable packet logging.
                                • enable
   override_quarantine          Choices:
   -                            • none            Quarantine IP or interface.
                                • attacker
   override_quarantine_expiry                     Duration of quarantine in minute.
   -
   override_quarantine_log      Choices:
   -                            • disable         Enable/disable logging of selected quarantine.
                                • enable
   override_rule_id                               Override rule ID.
   -
   override_status              Choices:
   -                            • disable         Enable/disable status of override rule.
                                • enable
   replacemsg_group                               Replacement message group.
   -

NOTES                   *fmgr_secprof_ips-notes*

   Note

     • Full Documentation at https://ftnt-ansible-docs.readthedocs.io/en/latest/.

EXAMPLES                   *fmgr_secprof_ips-examples*

 - name: DELETE Profile
   fmgr_secprof_ips:
     name: "Ansible_IPS_Profile"
     comment: "Created by Ansible Module TEST"
     mode: "delete"

 - name: CREATE Profile
   fmgr_secprof_ips:
     name: "Ansible_IPS_Profile"
     comment: "Created by Ansible Module TEST"
     mode: "set"
     block_malicious_url: "enable"
     entries: [{severity: "high", action: "block", log-packet: "enable"}, {severity: "medium", action: "pass"}]

RETURN VALUES                   *fmgr_secprof_ips-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key     Returned                                                                                  Description
   api_result always   full API response, includes status code and message
   string

STATUS                   *fmgr_secprof_ips-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Luke Weighall (@lweighall)
     • Andrew Welsh (@Ghilli3)
     • Jim Huber (@p4r4n0y1ng)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *fmgr_secprof_ips-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/fmgr_secprof_ips_module.html
