SYNOPSIS                   *osx_defaults*

     • osx_defaults allows users to read, write, and delete macOS user defaults from Ansible scripts.
     • macOS applications and other programs use the defaults system to record user preferences and other information that must be maintained when the applications are not running (such as default
       font for new documents, or the position of an Info panel).

PARAMETERS                   *osx_defaults-parameters*

       Parameter         Choices/Defaults                                                                             Comments
   array_add           Choices:
   boolean             • no ←                  Add new elements to the array for a key which has an array as its value.
                       • yes
   domain            Default:                  The domain is a domain name of the form com.companyname.appname.
   string            "NSGlobalDomain"
   host                                        The host on which the preference should apply.
   string                                      The special value currentHost corresponds to the -currentHost switch of the defaults commandline tool.
   key                                         The key of the user preference.
   string / required
   path              Default:                  The path in which to search for defaults.
   string            "/usr/bin:/usr/local/bin"
                       Choices:                The state of the user defaults.
   state               • absent                If set to list will query the given parameter specified by key. Returns 'null' is nothing found or mis-spelled.
   string              • list                  list added in version 2.8.
                       • present ←
                       Choices:
                       • array
                       • bool
   type                • boolean
   string              • date                  The type of value to write.
                       • float
                       • int
                       • integer
                       • string ←
   value                                       The value to write.
   raw                                         Only required when state=present.

NOTES                   *osx_defaults-notes*

   Note

     • Apple Mac caches defaults. You may need to logout and login to apply the changes.

EXAMPLES                   *osx_defaults-examples*

 - osx_defaults:
     domain: com.apple.Safari
     key: IncludeInternalDebugMenu
     type: bool
     value: true
     state: present

 - osx_defaults:
     domain: NSGlobalDomain
     key: AppleMeasurementUnits
     type: string
     value: Centimeters
     state: present

 - osx_defaults:
     domain: /Library/Preferences/com.apple.SoftwareUpdate
     key: AutomaticCheckEnabled
     type: int
     value: 1
   become: yes

 - osx_defaults:
     domain: com.apple.screensaver
     host: currentHost
     key: showClock
     type: int
     value: 1

 - osx_defaults:
     key: AppleMeasurementUnits
     type: string
     value: Centimeters

 - osx_defaults:
     key: AppleLanguages
     type: array
     value:
       - en
       - nl

 - osx_defaults:
     domain: com.geekchimp.macable
     key: ExampleKeyToRemove
     state: absent

STATUS                   *osx_defaults-status*

     • This module is guaranteed to have backward compatible interface changes going forward. [stableinterface]
     • This module is maintained by the Ansible Community. [community]

     • Franck Nijhof (@frenck)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *osx_defaults-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/osx_defaults_module.html
