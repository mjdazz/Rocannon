SYNOPSIS                   *scaleway_ip*

     • This module manages IP on Scaleway account https://developer.scaleway.com

PARAMETERS                   *scaleway_ip-parameters*

      Parameter          Choices/Defaults                                                                             Comments
   api_timeout      Default:                   HTTP timeout to Scaleway API in seconds.
   integer          30                         aliases: timeout
   api_token                                   Scaleway OAuth token.
   string                                      aliases: oauth_token
   api_url          Default:                   Scaleway API URL.
   string           "https://api.scaleway.com" aliases: base_url
   id                                          id of the Scaleway IP (UUID)
   -
   organization                                Scaleway organization identifier
   - / required
   query_parameters Default:                   List of parameters passed to the query string.
   dictionary       {}
                      Choices:
   region             • ams1
   - / required       • EMEA-NL-EVS            Scaleway region to use (for example par1).
                      • par1
                      • EMEA-FR-PAR1
   reverse                                     Reverse to assign to the IP
   -
   server                                      id of the server you want to attach an IP to.
   -                                           To unattach an IP don't specify this option
   state              Choices:
   -                  • present ←              Indicate desired state of the IP.
                      • absent
   validate_certs     Choices:
   boolean            • no                     Validate SSL certs of the Scaleway API.
                      • yes ←

NOTES                   *scaleway_ip-notes*

   Note

     • Also see the API documentation on https://developer.scaleway.com/
     • If api_token is not set within the module, the following environment variables can be used in decreasing order of precedence SCW_TOKEN, SCW_API_KEY, SCW_OAUTH_TOKEN or SCW_API_TOKEN.
     • If one wants to use a different api_url one can also set the SCW_API_URL environment variable.

EXAMPLES                   *scaleway_ip-examples*

 - name: Create an IP
   scaleway_ip:
     organization: '{{ scw_org }}'
     state: present
     region: par1
   register: ip_creation_task

 - name: Make sure IP deleted
   scaleway_ip:
     id: '{{ ip_creation_task.scaleway_ip.id }}'
     state: absent
     region: par1

RETURN VALUES                   *scaleway_ip-return values*

   Common return values are documented here, the following are the fields unique to this module:

      Key       Returned                                                                                   Description
                            This is only present when state=present
   data       when          Sample:
   dictionary state=present {'ips': [{'organization': '951df375-e094-4d26-97c1-ba548eeb9c42', 'reverse': None, 'id': 'dd9e8df6-6775-4863-b517-e0b0ee3d7477', 'server': {'id':
                            '3f1568ca-b1a2-4e98-b6f7-31a0588157f1', 'name': 'ansible_tuto-1'}, 'address': '212.47.232.136'}]}

STATUS                   *scaleway_ip-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Remy Leone (@sieben)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *scaleway_ip-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/scaleway_ip_module.html
