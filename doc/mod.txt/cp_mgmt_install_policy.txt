SYNOPSIS                   *cp_mgmt_install_policy*

     • install policy on Check Point over Web Services API
     • All operations are performed over Web Services API.

PARAMETERS                   *cp_mgmt_install_policy-parameters*

                 Parameter                Choices/Defaults                                                                  Comments
   access                                   Choices:       Set to be true in order to install the Access Control policy. By default, the value is true if Access Control policy is enabled on the
   boolean                                  • no           input policy package, otherwise false.
                                            • yes
   desktop_security                         Choices:       Set to be true in order to install the Desktop Security policy. By default, the value is true if desktop security policy is enabled on the
   boolean                                  • no           input policy package, otherwise false.
                                            • yes
   install_on_all_cluster_members_or_fail   Choices:       Relevant for the gateway clusters. If true, the policy is installed on all the cluster members. If the installation on a cluster member
   boolean                                  • no           fails, don't install on that cluster.
                                            • yes
   policy_package                                          The name of the Policy Package to be installed.
   string
   prepare_only                             Choices:
   boolean                                  • no           If true, prepares the policy for the installation, but doesn't install it on an installation target.
                                            • yes
   qos                                      Choices:       Set to be true in order to install the QoS policy. By default, the value is true if Quality-of-Service policy is enabled on the input
   boolean                                  • no           policy package, otherwise false.
                                            • yes
   revision                                                The UID of the revision of the policy to install.
   string
   targets                                                 On what targets to execute this command. Targets may be identified by their name, or object unique identifier.
   list
   threat_prevention                        Choices:       Set to be true in order to install the Threat Prevention policy. By default, the value is true if Threat Prevention policy is enabled on
   boolean                                  • no           the input policy package, otherwise false.
                                            • yes
   version                                                 Version of checkpoint. If not given one, the latest version taken.
   string
   wait_for_task                            Choices:
   boolean                                  • no           Wait for the task to end. Such as publish task.
                                            • yes ←

EXAMPLES                   *cp_mgmt_install_policy-examples*

 - name: install-policy
   cp_mgmt_install_policy:
     access: true
     policy_package: standard
     targets:
     - corporate-gateway
     threat_prevention: true

RETURN VALUES                   *cp_mgmt_install_policy-return values*

   Common return values are documented here, the following are the fields unique to this module:

            Key           Returned                                                                            Description
   cp_mgmt_install_policy always.  The checkpoint install-policy output.
   dictionary

STATUS                   *cp_mgmt_install_policy-status*

     • This module is not guaranteed to have a backwards compatible interface. [preview]
     • This module is maintained by the Ansible Community. [community]

     • Or Soffer (@chkp-orso)

   Hint

   If you notice any issues in this documentation, you can edit this document to improve it.

   ══════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

MORE INFO                          *cp_mgmt_install_policy-moreinfo*
>
All arguments are omni-completed, but if you really want to see the online docs:
https://docs.ansible.com/ansible/latest/modules/cp_mgmt_install_policy_module.html
